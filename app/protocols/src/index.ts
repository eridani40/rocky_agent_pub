/**
 * @app/protocols 入口 — 跨进程契约（IPC channel schema / DTO / 共享类型）
 * 参考: specs/tech/app/package/[P0]package_structure.md §2.1/§3.4
 *
 * v0.0.1 阶段为脚手架占位，具体契约在后续版本随业务落地补充。
 */

/** 包名常量，供各 workspace 自检引用 */
export const PROTOCOLS_PACKAGE_NAME = '@app/protocols';
