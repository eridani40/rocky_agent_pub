# v0.0.204 scope — studio-mate:parent:main
# extends default；显式独立成文件（后续按需分化）
extends: default
scopeId: studio-mate:parent:main
name: Studio Mate Main
description: studio mate 主 session scope（与 default 同链路；独立成文件便分化）
