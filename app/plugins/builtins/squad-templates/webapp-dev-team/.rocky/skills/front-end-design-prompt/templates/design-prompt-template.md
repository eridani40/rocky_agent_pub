# 前端设计 Prompt — {功能名}（{版本号} 增量）

> 用途：直接复制给外部设计工具（v0 / bolt / Make / Claude 等）产出可运行原型。
> 本 prompt **自包含**，设计工具无需读取本项目代码。

---

## ⚡ 本版增量（{版本号}，相对现状）— 重点

这是一次**增量改造，不是重做**。{一句话说明保留的外壳，如：AppShell 功能导航图标栏保留}。

| 维度 | 现状（当前） | 目标（本版增量） | 类型 |
|------|------------|----------------|------|
| {布局/结构维度} | {现状描述} | {目标描述} | 🔁 变更 |
| {新增能力} | {现状：无} | {目标描述} | 🆕 新增 |
| {bug 维度} | {现状：错误行为} | {目标：正确行为} | 🐛 修复 |

**保留不变**：{明确列出不变的部分，防止设计工具误改}。

---

## 背景

{应用类型 + 本次设计范围 + 涉及的页面}。技术栈 {React 19 + TailwindCSS 4 + ...}。请输出可运行的代码。

## 设计语言（必须遵守，token 值已给出，勿自行配色）

- 主色 `{hex}`（hover `{hex}`，浅底 `{hex}`）
- 辅助色：{...}；背景 `{hex}`；边框 `{hex}`
- 字体 {Inter}（正文）/ {JetBrains Mono}（id/代码）；基础字号 {14}px
- 圆角 sm={6} / md={8} / lg={10}；{主题切换方式 / 风格关键词}

## 全局骨架（ASCII 线框）

```
┌──────┬─────────────┬─────────────────┐
│ {栏1} │ {栏2}        │ {栏3}            │
└──────┴─────────────┴─────────────────┘
```
- 第 1 栏 {说明 + 选中态表现}
- 第 2 栏 {说明}
- 第 3 栏 {说明 + 滚动}

---

## 页面 A：{名称}

- {布局 / 分栏结构}
- {区域 1 详细要求}
- {区域 2 详细要求}
- **{关键交互约束}**（如：每个 group 独立保存；开关独立严禁联动）
- 数据 mock：
  ```json
  { "示例结构": "..." }
  ```

## 页面 B：{名称}

- {布局 / tab / 子分栏}
- {逐区详细要求，含控件类型分化}
- **{关键交互约束}**（如：radio 单选互斥 / checkbox 独立勾选 / 拖拽排序 + 独立开关）
- 数据 mock：
  ```json
  { "示例结构": "..." }
  ```

---

## 交付要求

1. {技术栈 + 组件方式}，mock 数据，无需真实网络请求
2. {响应式要求}
3. {关键交互必须可交互演示}
4. {schema-driven 表单 / 弹层等高级要求，如有}
5. 配色 / 圆角严格用上面 token；输出 dark / light 都协调
