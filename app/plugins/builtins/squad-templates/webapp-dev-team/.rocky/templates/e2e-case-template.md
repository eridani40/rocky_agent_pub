# ${CASE_ID} — {一句话功能验证目标}

> 这是 **case.md 模板**（PRD「关键用户路径」每条路径照此写一个 case——PRD 路径 = E2E case 源）。
> 纯自然语言，零断言零录制；executor 读 case.md + app-guide 按 snapshot 文案/位置自选定位方式。
> 替换所有 `{...}` 占位后删除本说明块。

## Use Case
作为{角色}，我想{用户意图}，验证{预期价值}——{链路定位，如「主链路贯通的最小冒烟」}。

## 前置条件
- env.sh 已起好环境（headless 或 electron 模式）。
- {外部依赖就绪条件，如：LLM provider 可用 / 已有种子数据 / 已登录某账号}。

## 操作目标（编号步骤）

1. **{动作短语}**：照 `${SPECS_DIR}/ui/overall/00-app-guide.md` §{章节号}——{导航路径描述}。
2. **{动作短语}**：{操作描述，含输入内容与目标控件}。
3. **{动作短语}**：{操作描述}。
4. **等待并验收 {结果}**：观察{位置}出现{预期可见结果}。
5. **（可选）{边界/多轮确认}**：{补充操作}。

## 验收口径（executor 自由心证）
- **pass**：{主链路成功标准，无瑕疵}。
- **small**：{能走通但有瑕疵的界定（不影响主路径）}。
- **blocking**：{走不下去的界定（报错/卡死/关键元素找不到）}。

## 依赖
- `${SPECS_DIR}/ui/overall/00-app-guide.md` §{章节号}（{页面}路径）
- `${SPECS_DIR}/ui/components/` 对应板块组件 spec（含可见文案，executor 按文案/位置自选定位方式）
