# v0.0.350 PRD — 国产 coding plan 四渠道 native 类型 + 额度/余额查询

> 版本日志 | 版本：v0.0.350 | 日期：2026-08-15 | 记录：prd（PRD 环节，编码前）
> worktree：`worktrees/v0.0.350-native-coding-plans`
> 需求：`reqs/[working] v0.0.350.native-coding-plans-and-balance-query.md`（老板 2026-08-14 22:00，主参考 cc-switch）
> 权威 PRD：`specs/prd/v0.0.350-native-coding-plans-and-balance-query.md`
> 调研输入：`specs/research/v0.0.350-coding-plans-balance-query.md`（预研）+ `specs/research/v0.0.350-live-verify.md`（四渠道真调全通）

## 1. 用户可感知变化（PRD 定义，待实现）

### 1.1 provider 类型选择器 +4 native 类型

- 既有「Anthropic 格式兼容」外新增：**Kimi Coding Plan / 智谱 GLM Coding Plan / MiniMax Coding Plan / DeepSeek（按量付费）**（用户可见名全友好词）；
- 选类型即**自动填充 baseUrl**（官方文档背书的 preset 默认值，可改）；
- protocol **固定 anthropic_messages 且不可改**（1 provider : 1 protocol 锁定，v0.0.53 既有规则）；
- 协议行为与 anthropic_compatible 完全一致（x-api-key + anthropic-version，四渠道实测 200 全通）。

### 1.2 额度/余额查询（列表外展示 + 5 分钟刷新）

- providers **列表页**底部新增「额度总览」列表级区块（provider 卡片之外，cc-switch SubscriptionQuotaFooter 模式）；仅 4 新类型实例参与；
- **每 5 分钟自动刷新**；失败保留上次成功值 + 「上次更新 HH:mm」（不整块消失）；重置倒计时文本每 30s 走动不重拉 API；
- 两种模板：**额度型**（kimi/glm/minimax）=「5 小时额度 已用 X%（HH:mm 重置）」+「本周额度 已用 Y%」两行；**余额型**（deepseek）=「余额 ¥9122.69」+ 不可用显示「余额不足」；
- **展开后内容（boss 铁律已定义）**：套餐名/会员档位 + 两桶完整明细 + 完整重置时间 + 分币种余额（ds）+ 上次更新时间；纯文本 + 百分比徽标渲染（无 markdown/富文本）。

## 2. 关键决策（调研三点形状 diff 消化）

| diff | 决策 |
|---|---|
| kimi 字段演进（字符串值 + `used` 直读 + membership 新字段） | **采纳**：字符串兼容解析 + used 优先 + membership.level 进展开区 |
| glm 发现第三类 TIME_LIMIT（unit:5 Web 工具分项） | **不采纳进本期**：按 cc-switch 口径忽略（不进 5h/周两桶），留后续增强 |
| glm 周桶 unit:6/number:1 变体 | **采纳**：只锚 unit 不锚 number（禁按重置时间排序，issue #3036 实证） |

## 3. 非目标

volc（需 AK/SK 双凭据）/ glm 团队版 / 国际站独立类型（baseUrl 可改即达）/ 手动刷新按钮 / 额度告警 / 用量历史曲线 / TIME_LIMIT 展示。

## 4. 交付与验证（doc-modifier2 补记，2026-08-15）

- **T1 `c184dbbd9`**（四渠道 native impl + queryQuota + 聚合端点，review `57cd240a4` PASSED）/ **T2 `fc6a311a7`**（类型 preset + 选择器联动 + 额度总览区，review `d9d56a4b9` PASSED）/ **S7 `1cc862d1e`**（plugin-config i18n 4 impl description 补录）。
- **验证**：UT 全量 **10718 passed** + `tsc -b` 0 error；**AT 5/5 pass**（quota 聚合临时 case + mr 冒烟回归）；ET et1/et2/et3 + et4 回归并行执行中（`states/v0.0.350/verify/e2e/`，结论以 ET 报告为准）。
- **偏离**：error_classify Partial 化（T1 报备）+ T2 表外接线 3 文件（key-choice-cards labels / config-crypto / config-sync-import）+ detail 307 行跨阈挂账下次拆——详见 `specs/tech/version_logs/v0.0.350/change_log.md`。
