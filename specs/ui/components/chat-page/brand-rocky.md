# Rocky 品牌（AgentAvatar / 名标 / app 图标）

> 层级: 跨层（component-message-stream 的 agent avatar + name + electron app 图标）
> 资产: reqs/v0.0.11/icon.png（Rocky 图标，hail mary project）
> 决策来源: reqs/v0.0.11/req.md + 用户决策③（Rocky 范围 = app 图标 + 机器人头像 + 名标「Rocky」）

## 视觉基线
- avatar：28×28 ，Rocky icon 占满（无 padding/边框；icon 自身已是完整品牌图）。
- name：「Rocky」Inter 10px/600 uppercase muted（沿用现 name 样式）。
- 对齐：仍  居中于头像列宽 w-9（双边对称不破坏）。
