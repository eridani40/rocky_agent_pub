---
type: log
title: Frontend KB 变更记录
updated: 2026-08-12
---

# Frontend KB 变更记录（ISO 倒序，最新在前）

## 2026-08-12 · v0.0.339（文件打开分流 — CSV/TSV 无条件系统打开 + 文本 >5MB 系统打开）

- **`index.md` ① 概念表 L42**：v0.0.339 openLocalPath 概念行升级——④ 文本分支三分流：① `csv`/`tsv` → **无条件系统打开**（不 stat、不内置，任何大小——表格文件交 Numbers/Excel）；② 其余文本 → `getSize` stat 大小判定（workspace→`GET /workspace/stat?path=`（A8 `statWorkspaceFile`）/ absolute→`rockyShell.stat`（A6 `shell:stat` IPC）；`statFile?` 注入优先（UT mock）；失败/无能力 undefined 降级内置）——**`size > TEXT_OVER_SIZE_BYTES`（= 5MB，导出常量）→ 系统打开**，`≤5MB` → `onEditor(format)` 内置；③ **图片分支不 stat**（无大小限制）。同步签名不变（消费方零改动：`section-workspace-panel.tsx` handleOpen + `component-message-stream.tsx` onLocalViewer 均零改动），系统打开复用 `openSystemWorkspace`/`openSystemAbsolute`。详见 `specs/tech/version_logs/v0.0.339-file-open-strategy/change_log.md` + `specs/prd/version_logs/v0.0.339-file-open-strategy.md`。

## 2026-08-11 · v0.0.329（门模型 — 2+3 三态 center/left/right，可横向滑动的门）

- **`index.md` ① 概念表 L49**：v0.0.329 门模型概念行——`use-preview-collapsed.ts` 三态 hook 重写（`DoorState='center'|'left'|'right'` + `pv-door-<sid>` 持久化 + 旧 `pv-collapsed-<sid>` 迁移/桥接 + **sessionId 变化重读门态 blocking 修复**）+ `layout-width-engine.ts` 可选 `chatCollapsed?` 分支（缺省旧路径逐字段相等回归保护；door=left 先 4 槽完整换算再门态重分配）+ `use-three-col-layout.ts` chatCollapsed 透传 + chatRenderWidth + `section-preview-area.tsx` 三态渲染（right=现状收起路径原样 / left=门框左缘粗线 rail+▶贴右+aside 占满无 resizer / center=细线左◀+右▶双把手）+ `component-preview-collapse-toggle.tsx` 可选 `direction/tooltipKey/testid` prop + page-chat/studio-chat-router Row 读 door + chatCollapsed 条件不渲染。**视觉 3 轮老板验收**（rail bg-bg-warm+双 border / rail 6→7px+handle 7→8px / stickCls 偏移 7→8px 同步）。详见 `specs/tech/version_logs/v0.0.329/change_log.md` + `specs/prd/version_logs/v0.0.329-region23-door.md`。

## 2026-08-11 · v0.0.328（纯文本打开 — 无扩展名/点文件白名单 + 搜索防抖 500ms）

- **`index.md` ① 概念表 L50**：v0.0.328 概念并入 324 行——`getFileFormat` 白名单扩充：`KNOWN_TEXT_BASENAMES` 精确集（Dockerfile/Makefile/.gitignore 等 12 项，扩展名查表前先查，大小写不敏感）→ 'txt'；`KNOWN_TEXT_STEMS` 词干集（Dockerfile.dev → stem='dockerfile' 命中，扩展名查表未命中时 fallback）；搜索防抖 300→500ms + 回车立即搜（跳过防抖）。详见 `specs/prd/version_logs/v0.0.328-text-file-open.md`。

## 2026-08-11 · v0.0.324（文件树搜索交互升级 — 裁剪式结果树）+ v0.0.327（搜索树可交互 — merge-expanded）

- **`index.md` ① 概念表 L50**：v0.0.324 裁剪树概念行（与 328 合并）——`ws-filter-tree.ts` `buildFilterTree` 纯函数（路径拆解补祖先段 / 同路径合并去重 / 命中目录用真实子项替换 / `expandedPaths` 只含祖先路径）；`component-ws-search-box.tsx` 瘦身（删结果列表 JSX，`onResult` 上报）；FileTree 常驻、搜索态数据源切裁剪树（PathBar 仍隐藏）；`SEARCH_LIMIT` 200→100；超限 → 底部 `tooMany` 截断提示。**v0.0.327**：`merge-expanded` action——`filterResult.expandedPaths` 合并入 `state.expanded`（不覆盖用户手动展开）；命中目录本身不自动展开。详见 `specs/tech/version_logs/v0.0.324/change_log.md` + `specs/ui/components/chat-page/component-workspace-panel.md §4.6`。

## 2026-08-10 · v0.0.320（文件预览区 — 三栏 chat|预览|工作区 + 多 tab + 编辑 + 冲突检测 + 弹层退役）

- **`index.md` ① 概念表 L48**：v0.0.320 文件预览区概念行——`layout-width-engine.ts` preview? 可选槽（preview=null 旧 3 槽路径零改动）+ `use-three-col-layout.ts` dragDynMax4 + `section-preview-area.tsx` / `preview-area-context.ts` / **`preview-area-provider.tsx`（Task 3 偏离：Provider 上移 page-chat/studio-chat-router 顶层包整行——workspace-panel/message-stream 是容器兄弟节点，Context 只能向下传；M-1 修复：sessionId 用 activeSessionId 与渲染面板对齐）** / `use-preview-tabs.ts`（tab 状态机：dirty 守卫三选 modal + 409 冲突 modal 取消=reload/覆盖=force；absolute 源 version='' 跳过冲突）+ `component-preview-tab-bar.tsx`（横滑 + chevron + × 关闭 + dirty ● + **展开态收起按钮 → rail 36px + localStorage**，ET-fix）+ `component-preview-viewer.tsx`（md→PrimitiveMarkdownView / 其余→pre）+ `component-preview-editor.tsx`（textarea + 保存/取消 + 错误保留重试）+ `component-preview-dirty-modal.tsx`/`component-preview-conflict-modal.tsx`（L3 modal，dirty 标题 {{name}} 插值 ET-fix）。**弹层退役（D13）**：删 `component-ws-file-editor.tsx` + `component-chat-link-viewer.tsx`（component-modal-md-editor 保留 academy）；无 Provider 降级 `component-ws-file-editor-fallback.tsx`。chat 链接 Provider 迁 `chat-link-handler-context.ts`（纯 TS createElement 断循环依赖）；message-stream onLocalViewer → openLocalPath 共享分流。workspace 文件树：文件夹 item 点击 toggle、`component-ws-search-box.tsx`（防抖 300ms 前端过滤 + 后端 searchWorkspaceFiles 补全）。file-format 'code' 分类（37 编程语言后缀 → 'code'，getCategory('code')='plain'）。范式归属：预览区全部控件即时操作 / L3 确认 modal / 拖拽手柄，不引入 SaveBar。

## 2026-08-08 · v0.0.286（Markdown Viewer 图片渲染）

- **`index.md` ① 概念表 L42**：v0.0.253 link-target 概念行扩展 v0.0.286——`isDangerousImageScheme`（图片专用危险协议判定，不改 isDangerousScheme：拦 javascript:/vbscript:/非 image data:，放行 data:image/ 白名单）+ `primitive-markdown-image.tsx` 新 helper（从渲染内核拆出 ≤300 行约束，范本 gfm-table）：block 级独立行 `![alt](url)` → `MarkdownImage` 三源分流（web 直渲 / data:image 直渲 / absolute readFileBinary IPC / relative joinPath resolve→workspace HTTP 或 IPC / 无 baseDir 降级 alt）+ 加载四态（loading/error/too-large/loaded）+ `PrimitiveImageLightbox`（Portal+遮罩+全尺寸 img+Esc/遮罩/✕ 三路关闭，本地+web 统一不走 WsImageViewer）+ 纯函数（resolveImageUrl/joinPath/deriveBaseDir）。modal-md-editor 加 filePath/sessionId 可选 props（deriveBaseDir(filePath)→baseDir）。
- **代码↔spec 核实（doc-modifier 阶段 5）**：① isDangerousImageScheme `link-target.ts L55` 拦 javascript:/vbscript:/data:(非image) + 放行 data:image/ ✅；② MarkdownImage 三源分流 `primitive-markdown-image.tsx` resolveImageUrl L58-67（web/data/absolute/relative 四分类）+ effect L135-192 按 type 分流（web/data 直渲不异步读 / absolute readFileBinary IPC / relative joinPath→workspace HTTP 或 IPC）✅；③ joinPath L24-31（空 baseDir 返 relative 原样，BUG-001 修复）+ deriveBaseDir L39-44（纯文件名→空串=根目录，BUG-001 修复）+ resolveImageUrl L63 `baseDir !== undefined` 判定（空串是有效根目录值，BUG-002 修复）✅；④ PrimitiveImageLightbox L72-103（Portal+遮罩+max-h-88vh+Esc/遮罩/✕ 三路关闭）✅；⑤ primitive-markdown-view.tsx block image 分支 L268（独立行 `^!\[...\]\(...\)---
type: log
title: Frontend KB 变更记录
 正则→MarkdownImage 渲染；baseDir/sessionId props 透传）✅；⑥ modal-md-editor.tsx L42-45 filePath/sessionId props + L207 `deriveBaseDir(filePath)` + sessionId 传 PrimitiveMarkdownView ✅；⑦ ws-file-editor L126-127 + chat-link-viewer L198-199 filePath=target.path+sessionId 传递链路完整 ✅；⑧ 2 BUG 修复（BUG-001 deriveBaseDir 纯函数替代有 bug 正则 + joinPath 空 baseDir；BUG-002 resolveImageUrl `baseDir!==undefined` 替代 falsy 判定 + readBinary DI prop）已核实 == 代码 ✅。

> 本目录级变更日志（位置轴）。跨版本发布说明（版本轴）见 `specs/tech/version_logs/vX.Y/change_log.md`。
> 一行一 feature；版本块尾指向该版本 change_log 详情。

## 2026-08-07 · v0.0.280（聊天链接打开行为统一 ≡ 右侧文件区 — openLocalPath 共享分发 + 去强制只读 + image/.url/absolute 分流 + 新 IPC 通道）

- **`lib/open-local-path.ts` NEW（151 行）**：共享本地文件分发 lib（聊天链 + 右侧 handleOpen 共用，老板铁律行为永远一致）。5 分支 × 2 源（workspace/absolute）：①folder→系统文件管理器 ②`.url`→嗅探浏览器（workspace→openRemoteLink / absolute→readFileText+parseUrlFileContent+openWebUrl；失败降级 txt editor）③image 6 格式→onImageViewer ④12 格式→onEditor(format) ⑤其余→系统打开。复用 file-format 单一权威（getFileFormat/isImagePath/isRemoteLinkPath），不新开格式集/图片白名单；kind=undefined（聊天链）跳过文件夹分支。basename 内部 5 行（file-format basename 私有不导出）。
- **`lib/link-target.ts`**：local 分支改调 openLocalPath（有 onLocalViewer → openLocalPath 回调，无 Provider → 降级 openPath 不变）；opts 加 `sessionId?`；web/dangerous 分支零改动。
- **`lib/remote-link.ts`**：`openWebUrl` 导出（断循环依赖——remote-link 不再 import link-target，openWebUrl ≡ link-target web 分支逐字等价）。
- **`chat-page/component-chat-link-viewer.tsx`**：**覆盖 v0.0.253 强制只读**——去 readOnly 传 onSave（workspace→saveWorkspaceFile / absolute→writeFileText IPC）+ 渲染分流（isImagePath→WsImageViewer source 透传 / 否则→ModalMdEditor format=getFileFormat ?? 'txt'）+ image 分支不读文本（直接清态走渲染分流——v0.0.253 遗留缺陷修复）+ 成功 toast「已保存」（2.6s flash）。
- **`chat-page/component-ws-image-viewer.tsx`**：WsImageTarget 加 `source?: 'workspace'|'absolute'`（缺省 workspace）；source='absolute' → readFileBinary IPC → base64 data URL（右侧 handleOpen 调用零变化）。
- **`chat-page/section-workspace-panel.tsx`**：handleOpen 改调 openLocalPath（行为零变化，五路分流语义原样保留）。
- **`common/primitive-markdown-view.tsx`**：linkOpts 构造带 sessionId。
- **electron IPC（`open-external-ipc.ts` + preload + rocky-shell.d.ts）**：新 `computeWriteFileText`（utf8 覆盖 last-write-wins；ENOENT→not-found / EACCES→permission-denied）+ `computeReadFileBinary`（stat 预检 >2MB→too-large；base64 返回）+ `shell:writeFileText` / `shell:readFileBinary` 两 channel（均先 computeResolveLocalPath 展开）。错误语义与 readFileText 逐字对齐。
- **偏离1（循环依赖断环）**：openLocalPath→remote-link→link-target→openLocalPath 环（vitest mock 下绑定 undefined）→ remote-link 去掉 link-target import（openWebUrl 内联导出，语义逐字等价）+ open-local-path 从 remote-link 复用。**偏离2（image 分支不读文本）**：change_plan 行 29 指非 image 分支；原 useEffect 无条件读文本是 v0.0.253 遗留缺陷，image 二进制 readFileText 失败挡 viewer → 修复后对齐右侧 handleOpen。
- 详见 `specs/tech/version_logs/v0.0.280/change_plan.md`（24 行 method 级表 + 6 裁决）+ `change_log.md`

## 2026-08-06 · v0.0.269（workspace 文件类型分流 + 团队状态浮菜单 — handleOpen 五路分流 + binary 通道 + squad-status modal）

- **`lib/file-format.ts`**：加 `IMAGE_EXTS`（6 格式闭合 png/jpg/jpeg/gif/webp/svg）+ `isImagePath(path)`（basename 扩展名大小写不敏感，无扩展名 false）——image 分流判定；`looksBinary` **保留**（注释更新：前置分流后不作主判定，仅 text 白名单内真二进制防御——`.txt` 改名成真二进制时占位 pill）。
- **`chat-page/component-ws-image-viewer.tsx` NEW（148 行）**：workspace 图片只读查看器——`WsImageTarget`（path/fileName/subtitle）+ `Props { sessionId, target, onClose }`（target null 不渲染）；L3 modal（Portal + modal shell + 遮罩/Esc/关闭三路）；打开 `readWorkspaceFileBinary` → `data:image/{ext};base64,` → `<img>`（max-w/max-h 保持纵横比）；mediaTypeFromPath 6 格式映射兜底 octet-stream；只读（无编辑/保存/格式化/校验）；失败轻量 error。testid：ws-image-viewer / ws-image-viewer-img / ws-image-viewer-error。
- **`chat-page/section-workspace-panel.tsx` handleOpen 五路分流（v0.0.263 二元 → v0.0.269 五路，顺序 MUST）**：`node.type !== 'file'`（文件夹）→ openWorkspaceItem(kind='folder')；`isRemoteLinkPath`（.url）→ openRemoteLink（嗅探失败降级 editor）；`isImagePath`（6 格式）→ `setWsImageTarget`（与 fileEditorTarget 互斥）；`getFileFormat !== null`（12 格式文本）→ setFileEditorTarget；其余（未知/非 6 格式图片）→ openWorkspaceItem(kind='file') 系统打开（**无占位 pill**）。**消灭「二进制无法预览」占位 pill**——前置分流后进 editor 的都是文本。
- **`lib/chat-api/workspace-api.ts` `readWorkspaceFileBinary` NEW**：`GET /session/:id/workspace/file?binary=1` → `{ content: base64 }`（服务端读 Buffer；白名单校验与文本同一路径安全面不变）。
- **`chat-page/component-chat-float-menu.tsx` 第 4 菜单项「团队状态」**（skills 下方、todo **上方**——todo 保持最后，共 5 项）：`useSquadStatus()` 读 SquadStatusContext（page-studio chat 分支 Provider 下传；**无 Provider → 按钮不渲染 fail-safe**）；running badge = `deriveRunningCount`（deployed isRunningState 计数含 leader，0 态 Badge 不渲染不占位）；`openModal` 扩 `'squad-status'`；props 加 `chrome?: SessionChromeView`（`currentMemberId = chrome?.memberId ?? undefined` 防套娃）。data-action-key="chat.squad-status.open"。
- **`chat-page/section-chat-session.tsx`**：渲染 float-menu 传 `chrome={chrome}`（currentMemberId 来源；studio 单聊=对端 member id，群聊/playground/academy=undefined）。
- **`studio-page/component-squad-status-modal.tsx`（v0.0.268 entry 改造）**：面板逻辑自 entry 迁本组件——L3 modal（Portal + modal shell + 遮罩/Esc/关闭三路）+ running 上 / idle 下分区（derivePanelRows）+ presence 文字 + hover 进入对话 icon + **防套娃**（`row.member.id === currentMemberId` → 不渲染进入对话 icon，行内容保留）+ 打开 refreshDetail fire-and-forget（ctx null 守卫前无条件调用，hooks 规则）+ 无 Provider 不渲染（双保险）。testid：squad-status-modal / squad-status-row-{memberId}。Props `{ onClose, currentMemberId? }`。
- **`studio-page/section-studio-chat.tsx` topbarLeft 恢复 268 前形态**：`SquadStatusEntry` 删除——单聊 = MemberAvatar+name+tag；群聊 = 缺省 ChatSessionTopbarLeft。`component-squad-status-entry.tsx` 删除（按钮逻辑并入 float-menu 第 4 项，面板逻辑迁 modal）；squad-status-utils/context/Provider 不动（数据注入 268 架构保持）。
- **测试**：`session-workspace-file.test.ts` +2（binary=1 → base64 还原 == 原 Buffer / 白名单不变 traversal→400 + missing→404）+ `file-format.test.ts` isImagePath 组（6 格式 true / 非 6 格式 false）+ `component-ws-image-viewer.test.tsx` NEW（img 渲染/svg media type/error/只读无编辑/关闭三路/target null）+ `component-chat-float-menu.test.tsx`（squad-status 项 + fail-safe + running badge）+ `component-squad-status-modal.test.tsx` NEW（分区/防套娃/refreshDetail）。
- **代码↔spec 偏离核实（doc-modifier 阶段 5）**：IMAGE_EXTS 6 格式闭合 + isImagePath 大小写不敏感 / handleOpen 五路顺序（folder > .url > image > text > 系统打开）/ binary=1 Buffer→base64 + 白名单不变 / viewer L3 modal + 只读 + testid 三件套 / float-menu 第 4 项（skills 与 todo 之间、todo 最后）+ chrome prop + fail-safe / modal 防套娃 currentMemberId + refreshDetail / section-studio-chat topbarLeft 恢复 268 前形态 全部与 change_plan 一致 ✓；偏离 2 项等价合理（viewer Props 实际 = `{ sessionId, target, onClose }`（target 对象而非扁平 path/fileName/subtitle，spec 已按实现修正）/ i18n wsImageViewer 键在 chat ns workspace 嵌套对象下 ✓）。`component-workspace-panel.md §4.4` / `component-chat-float-menu.md` / `section-studio-chat.md` / `component-squad-status-modal.md` / `component-ws-image-viewer.md` / `04-agent-session.md §2.6.7` / `00-app-guide.md` 已按实际实现同步；`component-squad-status-entry.md` 删除（代码已删）。
- 详情：`specs/tech/version_logs/v0.0.269/change_plan.md`（method 级契约）+ `specs/tech/version_logs/v0.0.269/change_log.md`；PRD `specs/prd/version_logs/v0.0.269.file_dispatch_nav_status/prd.md`

## 2026-08-06 · v0.0.267（Session 输入草稿缓存 — chat-slice drafts + useChatDraft 接线 + 草稿>prefill）

- **`store/chat-slice.ts`**：`DraftContent = string` type（serializeEditorContent 序列化字符串——与发送通道 onSend content 同构，mention 保真）+ `ChatSliceState.drafts: Record<sessionId, DraftContent>`（内存级，无 persist）+ `saveDraft(sessionId, content)`（空内容 `!content.trim()` → 删 key 等价清除；非空 spread 新建写；**值相同不 set** 防恢复回写触发订阅）+ `clearDraft(sessionId)`（key 不存在 no-op 幂等）——均不可变更新。
- **`chat-page/use-chat-draft.ts` NEW（70 行）**：`useChatDraft(editor, sessionId, initialContent, store?)`——**全部 getState 读/写不订阅** → 输入零 re-render；mount 恢复（草稿 > prefill：有草稿 → `restoreDraftContent` 置 injectedRef 跳过 initialContent；无草稿 → 既有 injectInitialContent）；返回 `saveDraft(ed)`（serializeEditorContent 写缓存）+ `clearDraft()`；`store` 可选参数 = 测试注入独立实例。
- **`chat-page/chat-composer-helpers.ts`**：新增 `restoreDraftContent(editor, content)`——`deserializeContentToParagraphs`（mention pill 保真）→ `insertContent(paragraphs)`；与 injectInitialContent 并列（editor 命令封装；ref guard / queueMicrotask 推迟由 useChatDraft 负责）。
- **`chat-page/component-chat-composer.tsx`**：原 initialContent effect（ref guard + empty check + queueMicrotask）**移入 useChatDraft**（单一出口，净 -10 行）；onUpdate 追加 `saveDraft(ed)`（编辑即写，含空内容自动清除）；handleSubmit 追加 `clearDraft()`（发送后显式清，不赌 clearContent 触发 onUpdate 的框架行为，双保险）；297 行（≤300 达标）。
- **测试**：`store/__tests__/chat-slice-draft.test.ts` NEW（11 用例：写/空清除/值相同不 set/clearDraft 幂等/不可变/DraftContent 形）+ `chat-page/__tests__/use-chat-draft.test.tsx` NEW（7 用例：有草稿恢复含 pill/无草稿 prefill/草稿优先/回写同值幂等/actions 语义）+ composer 集成 UT 追加草稿用例（mount 恢复/输入写缓存/发送清除/prefill 回归）。
- **代码↔spec 偏离核实（doc-modifier 阶段 5）**：DraftContent/drafts/saveDraft 空清除+值相同不 set/clearDraft 幂等/useChatDraft getState 不订阅+草稿>prefill+store 注入/restoreDraftContent deserialize→insertContent/composer onUpdate+handleSubmit 全部与 change_plan 一致 ✓；偏离 2 项等价合理（interrupt.test beforeEach 清 drafts 既有测试适配 / useChatDraft store 参数测试隔离注入，生产零影响）✓。`chat-composer.md` / `section-chat-session.md` / `[P0]component_architecture.md` / `00-app-guide.md` 已按实际实现同步，无静默偏离。
- 详情：`specs/tech/version_logs/v0.0.267/change_plan.md`（method 级契约）+ `specs/tech/version_logs/v0.0.267/change_log.md`；PRD `specs/prd/version_logs/v0.0.267.input_draft_cache/prd.md`
## 2026-08-06 · v0.0.268（Squad 成员状态导航 — SquadStatusContext + 入口面板 + memo 阻断级联）

- **`studio-page/squad-status-context.ts` NEW**：`SquadStatusContextValue`（`{ detail, memberStateMap, onEnterChat, refreshDetail }`）+ `SquadStatusContext`（createContext null）+ `useSquadStatus()`（无 Provider 返 null fail-safe）——入口组件数据注入契约。
- **`studio-page/squad-status-utils.ts` NEW**：`buildMemberChatNode(detail, memberId, t)`（从 SeatsPanel 抽公共 helper，tag 规则 leader→tagLeader / mate→tagSingle + squadId，member 不存在返 null）+ `deriveRunningCount(detail, memberStateMap)`（deployed 成员 isRunningState 计数含 leader，suspended/benched 不计）+ `derivePanelRows(detail, memberStateMap)`（running/idle 分区，benched 过滤，行 = member/isLeader/presence/statusTextSource——复用 use-seats-data 同源派生）。
- **`studio-page/component-squad-status-entry.tsx` NEW（187 行）**：入口 = squad 图标 + running badge（**0 态不显示数字**，绝对定位叠加不占文档流）+ 面板（running 上 / idle 下分区 + presence 文字 + hover chat icon opacity 保留占位 + 整行 button）+ 外部点击/Esc 关闭 + 打开 refreshDetail fire-and-forget + detail null → loading + 无 Provider fail-safe 不渲染。testid：squad-status-entry / squad-status-panel / squad-status-row-{memberId}。
- **`studio-page/squad-status-provider.tsx` NEW（86 行，C1 拆分）**：memberStateMap 派生（遍历 detail.members sessionId → stateMap[sid]，**值比较逐项比对返 lastRef 稳定引用**——非成员 SSE 引用不变）+ refreshDetail useCallback（selectedSquadId + reloadDetail）+ value useMemo——包 chat 子树，**不新增 SSE 订阅**（复用 page-studio 已订阅的 session_meta `_all`）。
- **`studio-page/page-studio.tsx`（275 → 295 行）**：chat 分支包 `<SquadStatusProvider detail stateMap onEnterChat reloadDetail selectedSquadId>`（仅 chat，seats 不包）；onBack 改 useCallback（`handleChatBack`，deps chatBackSquadId 派生值 + fallbackToSeats，稳定引用供 memo）；onEnterChat = `handleSquadEnterChat` useCallback（setMainView chat 语义）。
- **`studio-page/component-studio-chat-router.tsx`**：导出改 `memo(StudioChatRouterImpl)`——props（node/prefill/onBack）引用稳定 → **page-studio SSE re-render 不级联 chat 树**（消息区/输入区零 re-render）；内部 useChatChrome 订阅不受影响；key={sessionId} remount 保留。
- **`studio-page/section-studio-chat.tsx`**：topbarLeft 恒渲染——单聊 = SquadStatusEntry + MemberAvatar+name+tag；群聊 = SquadStatusEntry + 显式 ChatSessionTopbarLeft（readOnly 缺省 chrome.readOnly，与 defaultTopbarLeft 等价）。
- **`studio-page/component-seats-panel.tsx`**：buildMemberChatNode 改调 squad-status-utils 公共 helper（DRY，行为逐字节一致，签名保留）。
- **i18n**：`studio:squadStatus.ariaLabel`（成员状态）/ `ariaLabelRunning`（成员状态，{{count}} 个运行中）双语同步。
- **测试**：squad-status-utils.test.ts（13 用例）+ component-squad-status-entry.test.tsx（14 用例）+ squad-status-provider.test.tsx（8 用例，锁定「非成员 SSE 引用不变」验收点）+ section-studio-chat.test.tsx 断言更新（topbarLeft 恒渲染）+ seats 回归。
- **代码↔spec 偏离核实（doc-modifier 阶段 5）**：SquadStatusContextValue / useSquadStatus fail-safe / buildMemberChatNode DRY / deriveRunningCount 含 leader / derivePanelRows 分区 / badge 0 态 + 绝对定位 / aria-label / 面板 refreshDetail / 外部点击+Esc / memo 阻断 / onBack useCallback / memberStateMap 值比较 / Provider 仅 chat / topbarLeft 恒渲染 / seats helper 全部与 change_plan 一致 ✓；偏离 2 项等价合理（PanelRow presence 字段 = seats fallback 键依赖 / i18n +2 键 ariaLabel）；C1 拆分（page-studio 326→295 + squad-status-provider 86 行）等价合理，详见 change_log.md 偏离记录。`section-studio-chat.md` / `component-seats-panel.md` / `00-app-guide.md` / 新组件 spec 已同步，无静默偏离。
- 详情：`specs/tech/version_logs/v0.0.268/change_plan.md`（method 级契约）+ `specs/tech/version_logs/v0.0.268/change_log.md`；PRD `specs/prd/version_logs/v0.0.268.squad_status_nav/prd.md`

## 2026-08-06 · v0.0.263（workspace symlink 浏览 + 内置 editor 放开格式限制 — 链式授权 + handleOpen 新语义 + symlink 渲染 + 二进制降级）

- **后端 `session-workspace-path.ts` NEW（whitelistResolve 迁出 + 链式授权解析）**：step1 字符串前缀检查保留（挡 `../` + 绝对路径注入）；step2 从 realRoot 逐段 resolve——每段 `lstatSync` 判 symlink → 命中则 `realpathSync` 授权该目标为继续解析的根。workspace 内存在的 symlink = 用户放置 = 授权（目标可在 workspace 外）；4 处调用点（tree/open/watch/file）自动统一；无 symlink 段时与旧 realpathSync 等价（普通路径零行为变化）；broken symlink → not_found。watch/file 仅 import 路径改 `./session-workspace-path`（零逻辑改动）。
- **`session-workspace.ts`**：WsTreeNode 加可选 `isSymlink?: boolean` + `linkTarget?: string`（type 仍 statSync 跟随后的真实类型 'file'|'dir'，枚举不变）；handleWorkspaceTree 节点 `lstatSync().isSymbolicLink()` 判 isSymlink + `realpathSync` 算 linkTarget（仅 symlink 节点）；过滤按节点名不变。前端 `workspace-types.ts` 对齐同字段。
- **`lib/file-format.ts`**：新增 `isRemoteLinkPath(path)`（`.url` 大小写不敏感 → true）；isBuiltinEditable **保留原 12 格式语义**（link-target.ts markdown 链接点击分发 v0.0.253 契约不变），仅注释更新说明「workspace 文件树打开不再用它判定」。
- **`lib/remote-link.ts` NEW**：`parseUrlFileContent(content)`（正则 `/(https?:\/\/[^\s]+)/i` 首个命中 → URL；无命中 → null）+ `openRemoteLink(sessionId, path)`（readWorkspaceFile → parse → openLinkTarget(url) Electron shell.openExternal / window.open fallback；嗅探失败返 `{opened:false}` 供降级）。
- **`section-workspace-panel.tsx` handleOpen 新语义**：`type==='file'` → ① `isRemoteLinkPath` → openRemoteLink（失败降级 editor）② 本地文件**一律进 editor**（任意扩展名，含 symlink；`format: getFileFormat(path) ?? 'txt'`，fallback 从 'md' 改 'txt'——unsupported → plain `<pre>` 防二进制当 markdown）；`type==='dir'`（含 symlink→dir）→ openWorkspaceItem kind='folder' 不变（后端链式放行）。
- **`component-ws-tree-item.tsx` symlink 渲染**：isSymlink → ① link 角标（absolute 右上角叠加，`.ws-ico` 槽位固定宽 13px 不占位——布局稳定性 MANDATORY，`data-testid="symlink-badge-{path}"`）② tooltip `title="→ {linkTarget}"`（i18n `workspace.tree.symlinkTooltip`）③ 交互不变（symlink→dir 有 twisty 可展开；hover 打开照常；排序归属真实类型分组不变）。
- **`component-ws-file-editor.tsx` 二进制降级**：readWorkspaceFile 后 `looksBinary(content)`（NUL `\u0000` 或替换符 `\uFFFD` 占比 >5%）→ 占位 pill「二进制文件无法预览」（i18n `workspace.mdEditor.binaryUnsupported`），不渲染 editor modal。
- **i18n**：`workspace.tree.symlinkTooltip`（"→ {{target}}"）+ `workspace.mdEditor.binaryUnsupported`（en "Binary file cannot be previewed" / zh "二进制文件无法预览"）双语同步。
- **代码↔spec 偏离核实（doc-modifier 阶段 5）**：链式授权（step1+step2）/ broken→not_found / isSymlink+linkTarget 可选字段 / type 真实类型 / isRemoteLinkPath 纯函数 / isBuiltinEditable 保留 / parseUrlFileContent 正则 / openRemoteLink opened:false / handleOpen 分流 / format??'txt' / symlink 角标+tooltip / looksBinary >5% / i18n 双语全部与 change_plan 一致 ✓；唯一偏离 = isBuiltinEditable 语义保留（架构决策②明确记录 + 已上报 orchestrator，非静默）。2 条后端测试预期反转（symlink 越界 400→200）= 本版本预期行为变更。`component-workspace-panel.md` §4.4+§6.5 / api §2.6 / session_workspace §6 / manager watch 策略 / app-guide 已按实际实现同步。
- 详情：`specs/tech/version_logs/v0.0.263/change_plan.md`（method 级契约）+ `specs/tech/version_logs/v0.0.263/change_log.md`；PRD `specs/prd/version_logs/v0.0.263.workspace_symlink_browse/prd.md`

## 2026-08-06 · v0.0.262（聊天滚动引导气泡 + 自动滚动跟丢修复 — hook 签名扩展 + 内容签名 autoScroll + rAF 合并）

- **`use-message-scroll-pagination.ts` 返回签名扩展**：`{ onScroll }` → `{ onScroll, nearBottom, scrollToBottom }`（新增字段向后兼容）。`nearBottom` = `useState(true)` React state（onScroll 内 `setNearBottom(scrollHeight - scrollTop - clientHeight <= 120)`，setState 值去重防滚动事件风暴）；`scrollToBottom(behavior='auto')` = `el.scrollTo({top: scrollHeight, behavior})` + 同步 `nearBottom=true`（气泡点击滚底后即时消失；编程 scrollTo 不触发 scroll 事件需显式同步）。
- **autoScroll 触发语义 = 内容变化（跟丢修复核心）**：`autoScrollDeps` 由 caller 传内容签名 `${rows.length}:${textLenSum}`（渲染视图 rows 派生，tool-batch 无 text 跳过）→ 新消息到达 **OR** 既有消息内容增长（流式 `text_block_delta` 同 rows.length 内容变）都触发滚底；旧 `rows.length` 单维度是生成跟丢根因。hook 内 **rAF 合并节流**（cancel + requestAnimationFrame + cleanup cancel，每帧最多一次滚底）。三不变量保留（isLoadingMore 跳过 / wasLoadingMoreRef / nearBottomRef 门控）。
- **`component-scroll-guide-bubble.tsx` NEW（ScrollGuideBubble）**：`visible = !nearBottom && hasMessages`；文案 = runActive ? 「新消息」: 「回到底部」；absolute 定位不占文档流（布局稳定性）；visible 用 opacity/pointer-events 过渡不 unmount（动画平滑）；button 语义 + aria-label。
- **`component-message-stream.tsx`**：contentSignature useMemo（`${rows.length}:${textLenSum}`）→ autoScrollDeps；解构 nearBottom/scrollToBottom；scroll 容器外包 relative wrapper + absolute 挂气泡（scroll div className 原样保留，BaseChatPage 骨架零改动）。
- **`build-render-rows.ts` NEW（79 行纯函数）**：`buildRenderRows(elements, elementBatch, batches)` + `RenderRow` 类型导出——message-stream 超 300 行上限（code-review Critical）触发抽离，纯机械抽取零逻辑改动。
- **i18n**：`chat.json` 顶层新增 `scrollGuide` 对象（en + zh-CN 双语）：`newMessage` / `backToBottom` / `ariaLabel.{newMessage,backToBottom}`。
- **代码↔spec 偏离核实（doc-modifier 阶段 5）**：hook 签名 / 阈值 120 / rAF 合并 / 三不变量 / contentSignature（tool-batch 跳过）/ 气泡 visible / 不 unmount / button+aria-label / i18n 双语全部与 change_plan 一致 ✓；ariaLabel 嵌套键（原计划单键）语义等价 Minor 记录不修 ✓。`_overview.md` 新建 §4.5 滚动权威章节（代码注释既有引用补成文）+ 新组件 spec + app-guide 操作语义，无静默偏离。
- 详情：`specs/tech/version_logs/v0.0.262/change_plan.md`（method 级契约）+ `specs/tech/version_logs/v0.0.262/change_log.md`；PRD `specs/prd/version_logs/v0.0.262.scroll_guide_bubble/prd.md`

## 2026-08-04 · v0.0.253（markdown 链接点击分发 — link-target lib 单一权威 + ChatLinkHandlerContext 透传 + 内置只读 viewer 挂载）

- **`lib/link-target.ts` NEW（链接点击分发单一权威，纯函数）**：`isDangerousScheme`（javascript:/vbscript:/data: 拦截，从 `primitive-markdown-view.tsx` 原 `isSafeUrl` 提取——语义逐字一致不放宽不收紧，单一权威 DRY）+ `classifyLinkTarget`（dangerous 优先 → `file:` 显式归 local → web scheme `^[a-z][a-z0-9+.-]*:` → 其余 local）+ `toChatLinkTarget`（source=absolute/workspace 判定 + fileName basename）+ `openLinkTarget`（dangerous 不动；web→`window.rockyShell.openExternal`（非 Electron 降级 `window.open(target,'_blank','noopener')`）；local 12 格式（复用 `file-format.ts isBuiltinEditable`，review 修重复实现）→ `opts.onLocalViewer`（**无回调降级 `openPath` 系统打开**）；其它 local→`openPath`）。
- **`primitive-markdown-view.tsx`**：删本地 `isSafeUrl`→import `isDangerousScheme`；`renderInline` 加 `linkOpts` 参数贯穿；`<a>` 加 `onClick={e=>{e.preventDefault(); openLinkTarget(url, linkOpts)}}`（保留 href/target/rel；样式 `text-accent underline cursor-pointer` 不变）。`useChatLinkHandler()` 取 Context（无 Provider 返 null → linkOpts=null → 12 格式链接走系统打开降级）。
- **`chat-link-handler-context.ts` NEW（纯 TS 独立文件）**：`ChatLinkHandlerContext` + `useChatLinkHandler` + `ChatLinkHandlerContextValue`（onLocalViewer + sessionId）。独立成文件断循环依赖（primitive-markdown-view → context（纯 TS）← component-chat-link-viewer → ComponentModalMdEditor → primitive-markdown-view 若合并成环）。
- **`component-chat-link-viewer.tsx` NEW（内置只读 viewer 挂载层）**：`ComponentChatLinkViewer`（target 变化按 source 分流取内容：workspace→HTTP `readWorkspaceFile`（后端 whitelistResolve，不混 IPC）/ absolute→`window.rockyShell.readFileText`；递增 reqId 防竞态（范本 ws-file-editor）；`reason='not-found'`→i18n `linkViewer.fileNotFound` 其余→`linkViewer.loadFail`；内容就绪挂 `ComponentModalMdEditor` readOnly=true 强制 + `format=getFileFormat(path) ?? 'md'` 兜底 + 不传 onSave）+ `ChatLinkHandlerProvider` 便利包装。
- **`component-message-stream.tsx`**：加 `sessionId?` prop（可选，兼容 ~50 处既有 UT 调用；生产 caller `section-chat-session` 必传）+ `chatLinkTarget` state + `ChatLinkHandlerProvider` 包渲染树（value={onLocalViewer:setChatLinkTarget, sessionId}）+ 挂 `ComponentChatLinkViewer`。
- **`index.md` ①**：概念表加「link-target 链接点击分发」行。
- **代码↔spec 偏离核实（doc-modifier 阶段 5）**：分发语义与 PRD §3.2 一致（`file:` 归 local 是 coder 对 change_plan §E 的修正，符合 PRD 意图）；readOnly 强制 ✓；workspace 相对路径仍走 HTTP 未混 IPC ✓；危险协议拦截语义不变 ✓；无 Provider 降级 openPath 而非 silent no-op（change_plan §E 修正，符合 §G 降级语义）✓。无静默偏离。
- 详情：`specs/tech/version_logs/v0.0.253/change_plan.md`（E/F/G/H 节）+ `specs/ui/components/chat-page/component-chat-link-viewer.md`

## 2026-08-03 · v0.0.245（终端中断体验优化 — ESC 焦点门控中断 + 统一中断动作编排 + mention 反序列化器 + mention-tag.ts 抽取）

- **`app/web/src/components/chat-page/mention-tag.ts` NEW（mention parsing 原语单一权威）**：5 原语 `MENTION_RE` / `ATTR_RE` / `parseTagAttrs` / `unescapeAttr` / `escapeAttr` 从 `component-mention-render.tsx` + `chat-composer-extension.tsx` 平移（字面不变），改为 export；新增 `TiptapNodeJSON` 类型 + `deserializeContentToParagraphs(content)` 反序列化器。无 React / 无 Tiptap 依赖（纯字符串处理）。三类消费者共用：渲染侧（`component-mention-render.tsx`）/ 序列化侧（`chat-composer-extension.tsx::serializeMention` 用 `escapeAttr`）/ 反序列化侧（本文件 `deserializeContentToParagraphs`）。**反序列化器 INV-2 类型无关**：不含 `if(type===)` 分支，仅按 `attrs.icon/label/badge` 构 pill；旧格式（缺 `icon`/`label`）降级整段 tag 字符串作 text 节点（与 renderer 降级规则一致）；仅注入路径调用，**实时手打 `<mention/>` 即时识别显式不做**。
- **ESC 中断焦点门控（`component-chat-session-input.tsx` window capture listener）**：window `addEventListener('keydown', onKey, true)` **capture 阶段** listener，门控判定链 short-circuit：`!composerRef.isFocused()` → noop（焦点不在输入区，ESC 不中断；modal/body/消息流各自 ESC handler 照常）/ `composerRef.isPopoverOpen()` → noop（@ popover 开，让 composer 自管关）/ `pendingToolCall` → noop（HITL 卡 pending）/ `sessionRunning` → `preventDefault` + `handleInterrupt`。**焦点门控取代 overlay-root/modal 探测**：焦点在弹窗=不在输入区→ESC 自然不中断，省 DOM 查询（内联 modal 如 memory-editor-modal 盲区一并覆盖）。
- **统一中断动作（`handleInterrupt`）**：ESC + 红钮 `onAbort` 同 handler（语义统一 UC-A4）。4 步编排：snapshot enqueueItems（防 SSE 移项丢 content）→ 逐条 `cancelEnqueue`（fire-and-forget，移项靠 SSE `enqueued_message_canceled` INV-1/5）→ `composerRef.applyInterrupt(items)`（注入排队内容到输入区开头保留 mention pill + 焦点管理）→ `onAbort()` = `POST /session/:id/abort`。红钮 wiring 改 `() => handleInterrupt()`（原直接透传 `onAbort`）；section 仍传 `onAbort={() => runState.abort()}` 作 abort 原语。**零后端改动**（abort/cancel 既有端点）。
- **`ChatComposerHandle` 扩三方法**：`isPopoverOpen()`（读 `triggerRef.current` inline 同步不 stale）/ `isFocused()`（读 `editor.isFocused` 保封装）/ `applyInterrupt(items)`（中断注入，不调 `onSend` / 不 `clearContent`，与 `send()` 区分）。新增 `triggerRef` inline 同步 trigger 最新态（无 effect 链）。
- **`buildInterruptTransaction` 纯函数（`chat-composer-extension.tsx`）**：`(state, items) → { tr, newFrom, newTo } | null`。items.flatMap(`deserializeContentToParagraphs`) → `schema.nodeFromJSON` 构 PM Node[] → `tr.insert(0, nodes)`（position 0，原内容位置严格 > 0 → mapping 干净平移）；返 `mapping.map(state.selection.{from,to})`。`items.length===0` 返 null（caller 跳过 dispatch）。**caller `applyInterrupt` 负责分发 + 焦点管理**，本函数不调 `view.dispatch` / `editor.commands`。
- **焦点管理两分支（`applyInterrupt` 内）**：`wasFocused === true`（ESC 触发恒走——焦点门控前提即焦点在输入区）→ 捕获 selection `{from,to}` BEFORE mutation → `tr.setSelection(TextSelection.create(tr.doc, newFrom, newTo))` + `view.dispatch(tr)`（光标跟随原内容下移，相对偏移不变，不 blur）。`wasFocused === false`（红钮触发走——按钮点击使 editor 失焦）→ 仍 dispatch tr（`items.length===0` 跳过）+ `editor.chain().focus('end').run()`（焦点 + 光标到末尾）。
- **`chat-composer-helpers.ts` NEW（文件行数收敛 helper）**：`detectMentionTrigger`（从 `component-chat-composer.tsx::detectTrigger` 抽出的纯函数）+ `injectMentions`（initialContent mount-time 注入链）。因 composer 加 ChatComposerHandle 三方法后超 300 行硬上限触发抽离，**行为零改**。
- **两处 Tiptap v3.x 类型适配（behavior 等价，不改契约）**：① `editor.chain().focus('end').run()` 替 `setTextSelection('end')`（Tiptap v3.27.1 `setTextSelection` 类型签名收紧只接受 `number | Range`，`FocusPosition` 含 `'end'` 等价）；② `buildInterruptTransaction` 返 `Transaction` 类型 import from `@tiptap/pm/state` 替 `ReturnType<EditorState['tr']>`（`EditorState.tr` 是属性非方法，ReturnType 不适用）。
- **代码↔spec 一致（doc-modifier 阶段 5 核实）**：① `chat-composer.md` 新增「命令式句柄」「ESC 键路（焦点门控）」「中断注入（applyInterrupt + 焦点两分支 + 反序列化器）」「parsing 原语单一权威」四节——逐项与代码一致（ESC 4 分支顺序 ✓ / `tr.insert(0)+mapping.map` ✓ / cancel 移项靠 SSE 不进 store ✓ / 注入保留 pill ✓ / 红钮与 ESC 同 handler ✓ / 不 import `getOverlayRoot` ✓）；② `_overview.md §5.3` 中断项扩双触发 + 统一动作编排 ✓；③ `_data-flow.md §3.3` 加「中断动作 = 既有 abort+cancel 的产品层编排」子节 ✓。无静默偏离。
- **doc-sync**：`specs/ui/components/chat-page/chat-composer.md`（新增 4 节）+ `_overview.md §5.3`（中断项扩）+ `_data-flow.md §3.3`（中断动作编排子节）。
- **零变更（OUT OF SCOPE）**：后端 `abort` / `cancelEnqueue` 端点零改（既有契约）/ `serializeEditorContent` 输出侧零改（已对齐 `message-content.md`）/ `MentionRender` 渲染逻辑零改（仅 import 来源切换）/ `section-chat-session.tsx` 零改（仍传 `onAbort={() => runState.abort()}` 作 abort 原语）；`00-app-guide.md` 不动（ESC 是 chat 区键盘交互，非 nav-rail 入口路径，手册范围 §6 维护规则只覆盖板块入口/操作路径/功能链路）。
- 详情：`specs/tech/version_logs/v0.0.245/change_plan.md`（method 级契约 + 2 处 Tiptap v3.x 类型适配偏离已对齐本文档）；PRD `specs/prd/version_logs/v0.0.245.interrupt_exp/prd.md`

## 2026-08-02 · v0.0.241（内置 file viewer/editor 扩多格式 + 格式化/校验 — file-format 纯函数库 + 前端 isBuiltinEditable 守门）

- **`lib/file-format.ts` NEW（格式分类常量库，纯前端 UI 侧决策）**：`FileFormat` type union（12 形态 = 11 新 + md）/ `FileFormatCategory`（md/structured/plain）/ `FormatResult`（union：`{ok:true,output}` \| `{ok:false,error,line?,col?}`）+ `EXT_TO_FORMAT` 映射表（键全小写，含 `.yml`→yaml 双后缀；不含编程语言后缀 `.py/.js` 等）+ `getFileFormat(path)`（basename 取 lastIndexOf('.') 防多扩展名误判 + `.env`/`.env.*` 特判返 'env' + 大小写不区分）+ `getCategory(fmt)`（switch 闭合 12 case，default 'plain' 兜底）+ `isBuiltinEditable(path)`（= `getFileFormat(path) !== null` 便利函数，禁重复实现判定）。**为何不进 chat-api**：纯前端 UI 侧分类逻辑，与 chat-api 后端契约分离（后端 read/save 端点通用不限扩展名）。
- **`lib/file-format/{json,jsonl,yaml,xml,toml,csv,tsv}.ts` NEW（7 结构化格式 format/validate 纯函数）**：每种 `format*(text): FormatResult` + `validate*(text): FormatResult`，纯函数无 IO 无副作用。库选型：JSON/JSONL 原生 `JSON.parse/stringify`（V8 默认不转义中文）+ YAML 用已装 `yaml` ^2.9.0 + **XML 新装 `fast-xml-parser`** + **TOML 新装 `smol-toml`** + CSV/TSV 手写 RFC 4180 引号转义 + 列对齐。**`lib/file-format/index.ts`** = dispatcher（`formatText(format,text)` / `validateText(format,text)` 按 format 路由，md/plain 返 unsupported）。两新依赖落 `app/web/package.json`（持续可打包护栏：前端纯 JS 库，packaged 后端不引用 → 无 asar 风险，但必须进 workspace package.json 不能只进根）。
- **`XMLValidator.validate()` 偏离（vs XMLParser.parse）**：原 change_plan 计划 `validateXml` 用 `XMLParser.parse`，实际改用同库 `XMLValidator.validate()`——XMLParser 是「宽容」解析（`<unclosed>` 当自闭合），无法识别未闭合/错配标签，校验失去意义；XMLValidator 专为 well-formedness 校验，返结构化 `{err:{msg,line,col}}` 更符合「校验」预期。**formatXml 仍用 XMLParser**（宽容以尽量产出——校验失败也能格式化，用户要「格式化」非「校验」）。`fast-xml-parser` 同时 export 三者（XMLParser/XMLBuilder/XMLValidator）。
- **`component-modal-md-editor.tsx` 扩展为通用 file editor（文件名保留 md-editor 历史）**：Props 加 `format?: FileFormat`（缺省 'md' 向后兼容 academy，academy 不传 → 行为零改变）。派生 `category = getCategory(format ?? 'md')` 一次（避免多次调用）：① **view 按 category 分流**——md→PrimitiveMarkdownView（academy 回归保护）/ structured+plain→`<pre>` 朴素预览（无高亮/行号/折叠，PRD §2.2 极简）；② **edit 格式按钮**——edit + structured 显「格式化」「校验」两 BTN_SECONDARY，plain/md 用 **`visibility:hidden`** 渲染同尺寸占位（保布局稳定，禁 `display:none`+条件渲染致相邻位移）；③ **hint 状态机**（优先级链）：`saveError > validateResult.kind==='error' > validateResult.kind==='ok' > idle 默认 hint/versionLabel`。新增 `ValidateState` 三态（idle/ok/error）+ `handleFormat`/`handleValidate`。open effect 重置 validateResult 回 idle。
- **关键不变量：format/validate 失败不动 draft（防洗空坏内容）**：`handleFormat` 解析失败时**仅 setValidateResult error，不改 draft**（PRD §3.1）——解析失败本就产不出 output，若用空串/部分输出替换 draft = 洗空用户内容；校验失败**不阻塞保存**（last-write-wins 不变，用户可保存未通过校验的原文）。
- **挂载层改名 `component-ws-md-editor.tsx` → `component-ws-file-editor.tsx`**（chat-page/，仿 `component-academy-modals.tsx` 模式）：`WsMdTarget` → `WsFileTarget`（加 `format: FileFormat` 必填字段）+ `ComponentWsMdEditor` → `ComponentWsFileEditor`；向 `<ComponentModalMdEditor>` 透传 `format={target.format}`，读/存逻辑（readWorkspaceFile/saveWorkspaceFile/toast）完全不变。modal 标签名保持 `ComponentModalMdEditor`（modal 不改名 = 向后兼容 academy 引用 + i18n `mdEditor.*` key + 既有 UT）。**仅 `section-workspace-panel.tsx` 一处 import 牵连**（grep 全替换核实无残留）。
- **拦截点泛化（`section-workspace-panel.tsx handleOpen`）**：判定从 `node.path.toLowerCase().endsWith('.md')` 改为 `node.type==='file' && isBuiltinEditable(node.path)`（v0.0.227 的 `.md` 硬编码退役），命中后 `setFileEditorTarget({ path, fileName, subtitle, format: getFileFormat(node.path) ?? 'md' })`。三处 page 复用同一 SectionWorkspacePanel（chat-page / academy section-version-chat / studio section-right-tabs）→ 改一处全覆盖。
- **i18n（academy.json 中英）加 `fileEditor.*` 6 key**：`format` / `validate` / `formatFail`（格式错误，无法格式化）/ `validateOk`（✓ 格式正确）/ `validateFailLine`（第 {{line}} 行: {{msg}}）/ `validateFail`（{{msg}}）。**原计划 7 key 含 `formatOk`（已格式化），实际删 `formatOk`**——状态机只有 idle|ok|error 三态，format 成功走 `kind:'ok'` 显 `validateOk`（「✓ 格式正确」），`formatOk` 是死键无消费者（code-review 发现删除）。保留既有 `mdEditor.*` 不动（modal `useTranslation('academy')` 不变，避免破 academy 引用 + UT）。
- **代码↔spec 一致（doc-modifier 阶段 5 核实）**：① `component-modal-md-editor.md` Props.format ✓ / view 分流（md→PrimitiveMarkdownView / 其余→`<pre>`）✓ / 格式按钮（edit+structured 显，visibility:hidden 占位）✓ / hint 状态机（saveError>error>ok>默认）✓ / i18n key 数（spec 未写「7 key」，提及的 validateFailLine/validateFail/validateOk 均与代码一致，无 formatOk）✓；② `component-workspace-panel.md §4.4` 拦截分支（isBuiltinEditable 守门 12 格式 + md / 其余系统打开）✓ + 挂载层改名 `component-ws-file-editor.tsx` ✓ + `WsFileTarget` 加 format 字段 ✓；③ 后端 read/save 端点契约字段（path/content/ok）零改，仅 §2.6.7 注释扩「11 格式 + md 均走此端点（前端 isBuiltinEditable 守门），后端不限扩展名」。
- **doc-sync**：`specs/api/overall/04-agent-session.md §2.6.7`（注释扩 11 格式 + md 走此端点 / GET 行语义改「内置 file editor」）+ `specs/ui/overall/00-app-guide.md §3.1` workspace panel 行（11 格式 + md 操作路径 + 结构化 7 格式格式化/校验按钮）+ 本 `index.md` ① 加 file-format 概念行 + ④ 加原则 25（前端格式分类守门 + 后端不限扩展名）。
- **零变更（OUT OF SCOPE）**：后端 read/save handler `session-workspace-file.ts`（UTF-8 + 路径白名单，通用不限扩展名）/ §2.6.2 `POST /workspace/open`（其它扩展名仍走系统应用打开，回归保护）/ chat-api `workspace-api.ts`（readWorkspaceFile/saveWorkspaceFile 签名零改）；命名泛化（modal `md-editor` → `file-editor` + `mdEditor.*` → `fileEditor.*` i18n）留 follow-up（守 academy 向后兼容 + 既有 UT）。
- 详情：`specs/tech/version_logs/v0.0.241/change_plan.md`（method 级契约 + 4 处偏离已对齐本文档）+ `specs/tech/version_logs/v0.0.241/change_log.md`；PRD `specs/prd/version_logs/v0.0.241.md`

## 2026-08-02 · v0.0.239（workspace 文件树自然排序 — reducer ingest 落点 + 自定义分段比较器）

- **`lib/natural-sort.ts` NEW**：自然序比较器 util——`compareNaturalNames(a,b)` 拆交替「文字段+数字段」逐段比较（文字段大小写不敏感字符串序 / 数字段数值序 `90<100` / **同值不同格式按原 digit 字符串兜底** `'09'<'9'` 因 `'0'<'9'`，与 VSCode 一致）；`compareWorkspaceNodes(a,b)` 先按 `node.type` 分组（dir<file 文件夹置顶）再复用 `compareNaturalNames(name)`。**为何不用 `localeCompare(b, undefined, {numeric:true})`**：orchestrator 实测该 API 对 `9.txt` vs `09.txt` 返 0 不兜底；用户要确定性文字兜底（D 决策基线）。UT 21 case 覆盖边界：`90<100` / `a9<a10` / `09<9` / 大小写同序 / 纯数字 / 纯文字 / 混合前缀。`noUncheckedIndexedAccess` 下用 `s.charCodeAt(i)` 封 `isDigitAt`（越界返 NaN 安全）替代 `s[i]>='0'` 字符比较。
- **排序落点 = reducer ingest（非渲染层）**：`workspace-slice-reducer.ts` `setTreeLoaded`（顶层 state.tree）+ `setChildrenLoaded`（子目录 childrenCache[path]）两 ingest 点各 `[...arr].sort(compareWorkspaceNodes)` 复制后排序（不突变 caller 入参）。把「已排序」升级为 state invariant，满足 PRD 三条件：① 数据进渲染前必经排序（reducer 是 state 写入唯一入口）/ ② 缓存命中也有序（childrenCache 写入时已排，折叠再展开走缓存直接有序）/ ③ SSE stale 重取走同一 ingest（`applyWorkspaceFileChanged` 标 stale → effect 监测已展开父 → `onStaleRefetch` 重 GET → 又走 setTreeLoaded/setChildrenLoaded → 自动有序，无额外分支）。渲染层 `component-ws-file-tree.tsx TreeLevel` 零 `.sort`（grep 核实，避免双重排序）。
- **type 枚举核实**：`WsTreeNode.type = 'file' | 'dir'`（三处一致：`workspace-types.ts` L17 显式 / 后端 `session-workspace.ts` 写 `isDir ? 'dir' : 'file'` / `component-ws-file-tree.tsx` 已用 `node.type === 'dir'`）。
- **代码↔spec 一致（doc-modifier 阶段 5 核实）**：`component-workspace-panel.md §4.5`（coder 编码前置已补）声明与代码逐项一致——reducer 两 ingest 排序 ✓（grep 命中 2 处）、dir<file 文件夹置顶 ✓（`aIsDir ? -1 : 1`）、type 枚举 `'file'|'dir'` ✓、三条件 ✓。无静默偏离。
- **零变更（OUT OF SCOPE）**：后端 `handleWorkspaceTree`（`readdirSync` 原样返 FS 顺序，OS 字节序根源不动）/ HTTP client `getWorkspaceTree`（纯透传）/ 渲染层 `TreeLevel`（`items.map`）/ watch-SSE 机制（chokidar / event payload / stalePaths）；技能管理页 `components/common/file-tree.ts` 同问题（`localeCompare` 无 numeric）但 OUT OF SCOPE（用户铁律「只做 query 要求的」，需扩范围另立版本）。
- **doc-sync**：`specs/ui/overall/00-app-guide.md §3.1` workspace panel 行加文件自然序观察口径 + 组件契约 ref 补 §4.5；frontend `index.md` ① 加自然排序概念行 + ④ 加原则 24（reducer ingest = state invariant）。
- 详情：`specs/tech/version_logs/v0.0.239/change_plan.md`；PRD `specs/prd/version_logs/v0.0.239/change_log.md`

## 2026-07-31 · v0.0.228（todo 面板 SSE 实时化 — fanout 第三类扇出 + useTodoCrud 轮询退役）

- **`[P0]chat_area_hooks.md §4.2`**：useSessionPanelFanout 在 workspace/read 两类外加第三类 `session_todo_changed` → `store.setLastTodoEvent`（受控例外边界不变：ctx=null + onEvent 返 void；MUST NOT 处理 status/usage/summary/messages_cleared）。
- **`[P0]component_data_map.md`**：fanout 行加 todo 扇出；useTodoCrud 行改「SSE（经 store）+命令式」——onInit listTodos / refetch 静默刷（GET+mutateCtx，无 ctx-null/loading 闪烁——reload() 内部 setCtx(null)+setLoading(true)，SSE 高频触发禁用）/ store effect 匹配 sessionId 才 refetch。
- **前端 store**：chat-slice `lastTodoEvent` + `setLastTodoEvent`（幂等键=event.id 非 createdAt——同毫秒连写 createdAt 可能撞键丢事件，ulid 必唯一）；session-slice-reducer `SessionTodoChangedEvent` 变体（shape 与后端严格一致）。
- **UI（component-todo-modal）**：打开（挂载）弹层侧 refetch 一次（skills 先例）；尺寸 w-[520px]→w-[720px] max-w-[92vw] max-h-[88vh]；hover 收敛=主 item 行容器挂 hover（步骤层移出容器，弹层 top-full 主 item 正下方）；done 徽章 `--success`/`--success-bg` 绿（token 化零硬编码）。
- **代码↔spec 偏离核实（doc-modifier 阶段 5）**：use-session-panel-fanout.ts（todo case）+ use-todo-crud.ts（轮询代码全删无残留 / 静默 refetch / SSE effect）+ chat-slice.ts（幂等 id）+ component-todo-modal.tsx（尺寸/STATUS_STYLE/hover 结构/打开 refetch effect）与 spec 一致；无静默偏离。
- 详情：`specs/tech/version_logs/v0.0.228/change_plan.md` + `change_log.md`；PRD `specs/prd/version_logs/v0.0.228.md`

## 2026-07-31 · v0.0.227（workspace .md 拦截 + component-modal-md-editor 提升到 common/ — 架构设计落 spec）

- **组件提升（academy-page/ → common/）**：`component-modal-md-editor.tsx` 跨 ≥2 页复用（academy + workspace）触发 `academy-page/_overview.md §8.2` 提升预告落实。组件**行为/Props 零改动**（onSave 纯回调 `(newValue)=>Promise<void>\|void` 不变）；提升后改 2 处 import 路径：`../common/primitive-markdown-view` → `./primitive-markdown-view`（同目录）；`./academy-styles` → `../academy-page/academy-styles`（**common 反向引用 academy 样式 = 已知技术债**，follow-up 可把 BTN_PRIMARY/SECONDARY/ICON_BTN 提到 common 样式，本版本不顺手扩）。`../../lib/portal` 不变（common/ 与 academy-page/ 同深度）。academy 侧 3 处引用改 import 路径（component-academy-modals / component-skill-browser-modal / UT）。
- **workspace 拦截机制（`section-workspace-panel.tsx handleOpen`）**：加分支 `if (node.type==='file' && node.path.toLowerCase().endsWith('.md'))` → setMdTarget({path, fileName: basename, subtitle: 相对 workspaceDir 路径})；非 .md 文件 / 文件夹走原 `openWorkspaceItem`（回归保护 PRD §2.4）。`.md` 大小写不区分（PRD §6.4 裁决）。三处复用（chat-page/academy/studio）共用同一 SectionWorkspacePanel → 拦一处全覆盖。
- **挂载层 `component-ws-md-editor.tsx` NEW（chat-page/，仿 `component-academy-modals.tsx` 模式）**：props { target, onClose }；target 变化 → `readWorkspaceFile` 读内容 → 渲染 `<ComponentModalMdEditor>`（每次重开重新读，禁 stale，PRD §2.3）；onSave → `saveWorkspaceFile` 落盘（last-write-wins，PRD §6.3）→ 成功 flash toast「已保存」→ 失败 throw（组件保留 textarea 供重试）。`section-workspace-panel.tsx` 已 298 行近上限，挂载逻辑独立文件不塞 panel。
- **toast 范式 = 自造轻量（复用 studio `page-studio.tsx` 模式）**：`useState<string\|null>` + `flash(msg){ setToast; setTimeout(()=>setToast(null),2600) }` + 条件渲染 toast div。grep 确认项目无 toast 库 / 无 ToastProvider / 无 useToast（studio/skill-page 多处用此局部 state 范式）。零第三方依赖、零全局 toast 系统。PRD §6.2 toast 裁决落地为此范式（非引入 toast 库）。
- **HTTP client（`workspace-api.ts` NEW 2 函数，经 `chat-api.ts` barrel re-export）**：`readWorkspaceFile(sid, path)` GET `/session/:id/workspace/file?path=` → `{ content }`；`saveWorkspaceFile(sid, {path, content})` POST `/session/:id/workspace/file/save` → `{ ok: true }`。
- **后端 handler 拆独立文件 `session-workspace-file.ts`**（`session-workspace.ts` 已 298 行，对齐 `session-workspace-save-image.ts` 拆分先例）：复用 `session-workspace.ts` export 的 `json()` + `whitelistResolve()`（字符串前缀 + realpath 双层校验，挡 `../` + symlink 外部）。路由 `session-routes.ts` 加 `workspace_file` / `workspace_file-save` 两 sub 分发（仿 `workspace_save-image:150`）。API 契约 `specs/api/overall/04-agent-session.md §2.6.7`（本版本 architect 已补）。持续可打包护栏自检通过：纯 Node 内置 fs + 现有 helper，无新依赖 / 无 runtime-config 新键 / 无 plugin 改动，dev=packaged 行为。
- **技术债跟踪（follow-up）**：`common/component-modal-md-editor.tsx` 反向 import `../academy-page/academy-styles`（BTN_PRIMARY / BTN_SECONDARY / ICON_BTN tokens）—— common 依赖 academy-page 样式是层级倒置；follow-up 可把这批 BTN tokens 上提到 common 样式层（或 tokens 层）消除反向依赖。本版本不顺手扩（守最小改动 + change_plan 约束）。
- **doc-sync 已落（阶段 5）**：UI 组件 spec `common/component-modal-md-editor.md`（迁路径 + Props 对齐代码：`versionLabel`/`hint?`/`readOnly?`/`onSave` Promise 签名，`mode` 标内部 state）+ `chat-page/component-workspace-panel.md §4.4`（.md 分支）+ `academy-page/_overview.md`（§1 弹层 + §3 文件清单路径迁移）+ `specs/ui/overall/00-app-guide.md`（workspace .md 操作路径）+ api `04-agent-session.md §2.6.7`（architect 已补 file 读/存契约，doc-modifier 核对一致）。**仍待 follow-up**：`component_data_map.md` 加 ComponentWsMdEditor / readWorkspaceFile / saveWorkspaceFile 行 + `[P0]component_architecture.md` 目录树（common 移位 + chat-page 挂载层新增）—— 跨 KB 数据地图更新，建议单独 follow-up 一并补（含其他版本积累）。契约 `specs/tech/version_logs/v0.0.227/change_plan.md`；PRD `specs/prd/version_logs/v0.0.227.md`。

## 2026-07-29 · v0.0.216（chat 区域统一复用架构 — 实现落地 + doc-sync）

- **实现落地**：`section-chat-session.tsx`（+`component-chat-session-input.tsx` / `component-chat-session-topbar-left.tsx`（缺省身份 header `ChatSessionTopbarLeft`，T3 拆分偏离）/ `use-chat-chrome.ts` / `chat-actor-strategy.tsx`）交付，7 页全迁；删 `section-chat-detail` / `use-model-restore` / `use-subagent-run-refresh`（开放点闭合：丢帧根因已在 reducer 层根治，无需内部补拉）/ studio 5 文件 / academy 2 文件；列宽收敛 `common/use-persistent-width.ts` + `academy-col-widths.ts`；subagent 树扩 `flat`/`onOpenNode`/`openNodeLabel`/`terminatedLabel` 可选 props（academy 平行版删）。
- **doc-sync**：`component_data_map.md` §2（SectionChatSession/useChatChrome 行 + useRunState/useSummary enabled 门）+ §3（删除清单）+ §6 整节换 useChatChrome；`chat_area_hooks.md` §5 改 compose 单点；`component_architecture.md` 目录树/§3.1/§3.3/§3.5/§3.6/§3.7/§3.12 消费方名对齐；index.md 概念表两行更新。事后偏差全表见 `specs/tech/version_logs/v0.0.216/change_log.md`。

## 2026-07-29 · v0.0.216（chat 区域统一复用架构 — 架构设计落 spec）

- **`[P0]chat_session_assembly.md` NEW**：chat 统一装配层契约——`SectionChatSession`（自给型会话区，7 页唯一接入点）+ `useChatChrome`（GET /session/:id/chrome，Snapshot 形 GET-once）+ capabilities 门控 + 前端零 kind 分支 + `useRunState`/`useSummary` enabled 门。取代 `useStudioChatChrome`/`useModelRestore`/academy 自建装配层。接口权威 `specs/api/overall/04a-session-chrome.md`；实现契约见 `specs/tech/version_logs/v0.0.216/change_plan.md`。
- index.md：④ 加原则 23（统一装配层）；⑤ 导航加 chat_session_assembly 行。
- 待实现后 doc-modifier 同步：`component_data_map.md` §2（删 useStudioChatChrome/§6，加 useChatChrome 行）+ `chat_area_hooks.md` §5（三页 compose → SectionChatSession 单点）。

## 2026-07-20 · v0.0.182.ui_reactive（chat/studio 三栏响应式布局引擎，纯前端零后端）

- **`lib/layout-width-engine.ts` NEW（纯函数，UT 主战场）**：chat/studio 三栏宽度统一换算引擎——渲染宽 = clamp(静态min, min(设定宽, 动态上限), 静态max)，解析先 R 后 L；双场景分离（拖拽守 480 + 对侧 hold 上一帧 / 缩窄守 C_defend=clamp(480, 中部当前宽, 932)）；中部 ≥480 不变式 + 横滚兜底（内行 min-width=L+480+R + 页根 overflow-x-auto）；相位表 P0~P4 零硬编码由公式涌现。宽度常量（232/560/272/36、180/400/220、480/932）唯一权威源迁入引擎。
- **`use-three-col-layout.ts` hook NEW（chat-page/，page-chat 与 StudioChatRouter 共用）**：available（ResizeObserver + jsdom fallback）+ convWidth（localStorage 全局 `conv-panel-width`，左栏新增可拖 180~400 默认 220）+ rightReport（ws-panel 上报）+ dragging + 三 ref 帧回填。「状态在下、算术在上」——ws-panel 宽度 state/持久化仍自管，父引擎经 renderWidth prop clamp。
- **`component-col-resize-handle.tsx` NEW 通用拖拽手柄**：delta 算法（startWidth ± Δ clamp 场景 A 动态上限）消除「脱手」死区；`component-ws-resize-handle.tsx` 改薄 wrapper（保 testid `ws-resize`）；conv-panel 右缘新增手柄（**不带 testid**——用户裁决 2026-07-20，复用 .ws-resize 视觉模式）；i18n chat ns 加 `convPanel.resize.*`（双语）。
- **spec 同步**：`[P0]component_architecture.md` 新增 §3.13；UI 组件 spec 三份（_overview §4.1 / component-workspace-panel §4.2 / section-right-tabs）由 coder 编码前置更新。PRD `specs/prd/version_logs/v0.0.182/change_log.md`；契约 `specs/tech/version_logs/v0.0.182/change_plan.md`。
- 零后端 / 零 API / 零 SSE / 零新依赖；base-chat-page / app-shell / nav-rail / studio-sidebar（224 固定）不动。

## 2026-07-19 · v0.0.176.hanging_ui_clear（HITL 悬挂卡片随 session running 状态清除，纯前端零后端）

- **`use-messages.ts` session_panel 分支增 RUNNING_STATES 清 HITL**：新增 `RUNNING_STATES = new Set(['running','interrupting'])` 常量（对齐 useRunState sessionRunning 口径，**排除 suspended** 保 INV-2）；`session_status_update` 且 state ∈ RUNNING_STATES 且 `pendingToolCall` 非空 → 清 `pendingToolCall`（幂等）。治悬挂：子 agent / 另一个 tab / 后台激活让 session 进 running 时，原 HITL 已失效但卡片不消失。触发源是 session_status_update（sessionRunning 权威源），**非** agent_loop 消息（消息可能来自别处）。覆盖 ask question 卡（need_feedback）+ 权限卡（need_approval），因两卡都 gate 在 pendingToolCall（base-chat-input-bar §4）——单点修两卡。
- **spec 同步**：`[P0]chat_area_hooks.md §3` onEvent 分支表加 RUNNING_STATES 清 HITL 一行；UI 两卡 spec（`component-pending-question-card.md` / `component-pending-approval-card.md`）§2 加「session 转 running 强制清 HITL」invariant + INV-2 保留说明。自治合规：本修复是 useMessages 自身在 session_panel 分支内清自己的 pendingToolCall 字段，不跨 hook 调用（§6.2 决策不变）。
- 零后端改动；零新 testid；零新 SSE 订阅；UT 加 4 case（running 清 / interrupting 清 / suspended 不清 / 幂等跳渲染）。

## 2026-07-19 · v0.0.174.squad_spinner（团队视图成员 name 后 running spinner，纯前端零后端）

- **`use-seats-data.ts` 加 `isRunning` 字段 + 新 export `isRunningState(s)`**：`SeatRow` 接口加 `isRunning: boolean`；seatsRaw 派生加 `isRunning: isRunningState(sessionState)`（sessionState 来自 `stateMap[m.sessionId]`，stateMap 由 `useStudioUnreadMeta` 提供）。`isRunningState(s) = s === 'running' || s === 'interrupting'`——**deliberately 排除 suspended**（INV-2：suspended = loop 已退出等用户回填，亮「?」非 spinner）。与已有 `isBusyState`（含 suspended，用于 `presence='busy'` + `inProgressCount` 统计）**有意分离**，禁合并。
- **`component-seat-row.tsx` + `component-seat-card.tsx` name 后挂 SpinnerRing**：mate 行 / 队长卡 name 文本后条件渲染 `<SpinnerRing size="sm" testid="seat-card-${member.id}-running-spinner" />`（仅 `row.isRunning=true` 时）。复用现成 `common/spinner-ring.tsx`（不新写）——与 conv-item (12×12 default) / subagent-tree (10×10 sm) 同源；seat 两处用 sm 对齐紧凑上下文。`shrink-0` 占位防位移 INV-9；队长卡顺序 = 名 → spinner（if）→ LEADER badge。
- **spec 同步**：`component-seat-row.md` v1.1 + `component-seat-card.md` v1.5 + 本 `index.md` SeatsPanel 概念行补 isRunning 派生说明。
- 零后端改动；零新 testid 族（复用 `seat-card-{memberId}` 族加 `-running-spinner` 后缀）；零新 SSE 订阅；零新 i18n key。

## 2026-07-18 · v0.0.170.squad_home_ui（Studio 坐席页 C 紧凑指挥台风格重构，功能全保留，纯前端零后端）

- **seats tab 主体双列化（`component-seats-body.tsx` 重写）**：从「SeatStats 4 大卡 + TeamEntryRow 2 卡 + leader 独立行 + mates 3 列卡网格 + 末尾『+』虚线卡」改为双列指挥台 `seats-console`（grid `296px + 1fr`）——左列 `seats-side`（队长 mini 卡 + stats 2×2 + team compact links）+ 右列 `seats-roster` 白卡（roster 头：计数 + 「＋ 新增成员」按钮 `seat-add-card`；mate 行列表 `seats-mates-grid`；mates=0 → 体内 `seats-empty`）。视觉契约 `reqs/[working] v0.0.170.squad_home_ui/design-c-console.html`。
- **`component-seat-card.tsx` 重写为队长 mini 卡**：seclabel「队长」+ mini 行（avatar lg / 名 + 行内 amber LEADER badge / meta 行 = 脉冲点 + `statusText · state`）+ 操作行（enter flex-1 solid + more outline icon）。leader 标识改行内 amber badge（`--hue-amber-bg`/`--hue-amber`）——旧 v0.0.168 的 `--border-strong` 描边 / `shadow-sm` / `border-t-2` 强调条全废。**meta 行「最近活跃」删除**（`seat-card-{id}-last-active` testid 废）。
- **`component-seat-row.tsx` NEW（153 行）**：mate 坐席行 `SeatRowView`（命名避与 `use-seats-data` 的 `SeatRow` type 撞名）——avatar md + presence → who 列（名 + `role · state`）→ status 列（脉冲点 + statusText，fallback 弱化）→ ops 列（enter + more；**恒渲染 opacity-0，group-hover/focus-within 揭示**，布局稳定 + 键盘可达）。与 SeatCard 共用 `seat-card-{memberId}` testid 族（无 `-badge-leader`）。
- **`use-seat-menu.ts` + `seat-present.ts` NEW（卡/行共享抽离）**：`use-seat-menu`（94 行）= 菜单开关 state + 触发按钮 rect 定位 + `deriveMenuOpenUp` flip-up + `setTimeout(0)` 延迟挂 window click/contextmenu/Escape 关闭监听；`avail` 类型用 `ReturnType<typeof deriveMenuAvail>` 派生（规避 TS4058，不改菜单文件导出面）。`seat-present`（31 行）= `pulseStyle` 静态脉冲点（box-shadow 光晕，无 @keyframes INV-3）+ `useSeatStatusText`（currentWork 优先 / i18n 兜底）。
- **`component-seat-stats.tsx` 重写为 2×2 无缝格**：`grid-cols-2 gap-px` 缝色底（`bg-border`）+ 白格 `rounded-xl overflow-hidden`；每格 = 数字 18px/700（`-num` 派生 testid，大数 ≥1000 缩写 `12.3k`）+ label 11px muted；**图标/hue 下线**（`seat-stats-{*}-icon` 废）；null 字段数字位「—」dim 不隐藏格。
- **`component-team-entry-row.tsx` 重写为 compact links**：2 大卡横排 → 白卡内纵向 link 列表（每 link = 24px `IconBox` hue icon + 标题 + chevron-right；hover `surface-2`）；群聊 link 右键上抛不变。容器 testid `seat-team-entry-row`。
- **页头 C 化（`component-seats-panel.tsx`）**：删 header avatar（`seats-header-avatar` 废）；online badge 去边框改绿字圆点（`--presence-online`）；tabs 改下划线式（`-mb-px border-b-2` 压住 header 根 border-b；内容器只留 `pt-2.5` 无 pb）。
- **`use-seats-data.ts` 下线 `listSessions()`**：它是 meta 行 lastActiveIso 唯一消费者，meta 行删除后无消费方；现只调 `getBudgetUsage()`（limit=-1/失败 → null 降级）。presence/statusText 派生规则不变。
- **i18n 清理（studio.json 中英）**：删 `seats.sectionTeam` / `seats.addCard.hint` 孤儿 key。
- 详情：`specs/ui/version_logs/v0.0.170/change_log.md`（UI 变更总账 + testid 增删表）+ `specs/tech/version_logs/v0.0.170/change_plan.md`。

## 2026-07-17 · v0.0.168.ui_opt（Studio 首页 IA 收敛为单页中枢 + 侧栏树删除 + 导航收敛，纯前端零后端）

- **SeatsPanel 升级为唯一首页 landing（`studio-page/component-seats-panel.tsx`）**：header 3 tab（seats/panel/autowork）从 v0.0.165 的「切主区路由态」改为**本组件内 activeTab 切主体**——`seats`→ SeatsBody（stats/team-entry/leader 独立行/mates 网格/「+」卡）/ `panel`→ ManageTab（元信息+charter+危险区，内联）/ `autowork`→ AutoworkTab（toggle+heartbeat+budget+history，内联）。Props 新增 `onEditMember`/`onBenchMember`/`onDeployMember`/`onHire`/`onSaveMeta`/`onSaveCharter`/`onDelete`/`initialTab?`；删 `onSwitchToPanel`/`onSwitchToAutowork`（tab 本地态）。SeatsPanel 宿主右键浮层 state（`{sessionId,x,y}`），透传给 `component-studio-context-menu` primitive。原 328 行超 300 → 拆 `component-seats-body.tsx`（193 行，seats tab 主体）保 SeatsPanel 主容器 ≤300。
- **SquadPanel 3-tab 容器 + 成员 tab 整体解体**：`section-squad-panel.tsx` + `component-members-tab.tsx` + `component-member-card.tsx` 全部 mv `soft_deleted/v0.0.168/`（不 @deprecated / 不留僵尸）。旧「成员 tab」能力：deploy/bench/edit 迁到坐席卡「更多」菜单 + 「新增成员」迁到坐席网格末尾「+」虚线卡。`MainView { kind:'panel' }` 路由态取消；`page-studio.tsx` MainView 类型从 v0.0.57 四态收敛为 v0.0.168 四态（seats/board/chat/member，无 panel）。`use-squad-mutations.ts` 删 `setTabAdmin` prop（无 admin tab 主区需重置）+ 改名 `fallbackToPanel` → `fallbackToHome`（语义修正，回落首页 seats）+ 删 `bump: () => void`（原用于清 sidebar detail 缓存，v0.0.168 sidebar 无缓存后死代码）。
- **坐席卡「更多」按钮激活为菜单弹层（`component-seat-card.tsx` + `component-seat-card-menu.tsx` NEW）**：v0.0.165 disabled 占位「更多」按钮激活为 popover menu——菜单项 = 编辑（`onEdit` 提供时）+ bench（**仅 !isLeader && state==='deployed' && onBench 提供**，硬规则双层拒 leader 无 bench）+ deploy（**仅 state==='benched' && onDeploy 提供**）。SeatCard Props 新增 `onEdit?`/`onBench?`/`onDeploy?`/`onContextMenu?`（右键上抛父级复制 sessionId 浮层）；leader 卡强 highlight：原 `--border-strong` 基础上加 `shadow-sm` 常态 + `border-t-2 border-t-fg` 顶端强调条（中性色不违反 palette 白名单，无动画）。菜单开关本地 state + `setTimeout(0)` 延迟挂 window click/contextmenu/Escape（memory `dropdown-close-listener-defer-register`）。SeatCard 加菜单后原 305 行超 300 → 菜单 popover + `deriveMenuAvail` 纯函数拆到 `component-seat-card-menu.tsx`（primitive 层级，228 行）。
- **侧栏手风琴展开树彻底删除（`studio-page/section-studio-sidebar.tsx` + `component-squad-tree.tsx`）**：`component-squad-tree.tsx` mv `soft_deleted/v0.0.168/`；`section-studio-sidebar.tsx` 简为「新建按钮 + squad 单行列表」——`SquadRow` role=button+tabIndex=0+Enter/Space 键盘可达（无 `aria-expanded`——无展开态）。StudioSidebarProps 从 v0.0.137 的 ~11 prop 减到 4 prop（`squads`/`selectedSquadId`/`onSelectSquad`/`onNewSquad`）；删 `dataVersion`/`onOpenBoard`/`onOpenChat`/`unreadMap`/`activeChatSessionId`/`stateMap`（tree 消费方全废）。看板/群聊/单聊入口全部迁到首页坐席面板（团队入口卡 `seat-team-entry-{board,groupchat}` + 坐席卡 `seat-card-{id}-enter`）。**过渡期承认无侧栏可见未读回显**（新可见位置留后续版本）；`use-studio-unread-meta` 保留（stateMap 仍由 `use-seats-data` 用于 presence 派生，只删 sidebar 消费点）。
- **右键复制 Session ID 菜单迁址（`component-studio-context-menu.tsx` NEW primitive）**：从 v0.0.129 `section-studio-sidebar.tsx` 内嵌实现抽出为共享 primitive；触发点迁到**坐席卡右键**（`seat-card-{memberId}` 上抛 `onContextMenu(sessionId, x, y)`）+ **首页群聊入口卡右键**（`seat-team-entry-groupchat` 上抛 `onGroupChatContextMenu(x, y)`，sessionId 由父级用 `detail.squadChatSessionId` 补齐）；宿主 state 落 `SeatsPanel`（就地原则）。testid `studio-context-menu` / `studio-context-menu-copy-id` 沿用，i18n key `studio:sidebar.copySessionId` 沿用。`setTimeout(0)` 延迟挂 window click/contextmenu/Escape 关闭沿用。
- **看板路由头部新增返回键（`component-studio-board-route.tsx`）**：`BoardRoute` Props 新增可选 `onBack?: () => void`；头部左上渲返回按钮（`board-topbar-back-btn` testid，视觉复用 `ChatTopbarBackBtn` primitive）；点击回首页 seats（page-studio 侧 `fallbackToSeats` 回落 `{kind:'seats', squadId}`）。触发场景 = 侧栏树删除后看板唯一入口=首页坐席面板团队入口卡。布局稳定：`mb-2` 固定占位，不推挤下方 squad 名标题。SquadBoard 组件内部零改动。
- **chat 返回键常驻 + 单聊头像纯身份展示（`component-studio-chat-router.tsx` + `section-{squad,member}-chat.tsx`）**：`MainView.chat` 去 `from` 字段（来源门控删——chat 入口只剩首页 → 无来源分歧）；`StudioChatRouter` Props 从 5 个减到 3 个（`node`/`prefill`/`onBack`），删 `from`/`onOpenMember`；`showBackButton` 全链路删除，`section-{squad,member}-chat.tsx` 改「`onBack` 存在即挂返回键」。**单聊角色头像**（`squad-chat-role-avatar`）从 `<button onClick={onOpenMember}>` → `<div>` 纯展示——保留 testid + name + tag 供 ET 观测，删 hover/cursor-pointer；`MemberChatPage.onOpenMember` prop 全链路删除（唯一 member 编辑入口 = 首页坐席卡菜单编辑）。INV-6 沿用：同 mount 内 `onBack` 引用固定，chat 节点切换=remount 天然稳。
- **MemberPanel 返回目标简化（`section-member-panel.tsx`）**：`onBack` 恒调 `fallbackToSeats` + `reloadDetail`（不再按 fromChatNode 判断回聊页 vs 首页）；`MainView.member` 的 `fromChatNode` 字段删除（单聊头像编辑入口删后，member panel 唯一入口 = 坐席卡菜单编辑，回落首页 seats 是唯一合理归宿）。
- **ChatNode 类型独立（`chat-node.ts` NEW）**：原定义在被删的 `component-squad-tree.tsx`；抽出后被 page-studio / SeatsPanel / StudioChatRouter / SeatsBody 6 处 import 更新。
- **i18n keys 清理**：删除 `squadTree.board` 孤儿 key（中英两份；侧栏树删后无消费者）。`squadTree.tagLeader`/`tagSingle`/`tagGroup`/`groupChat` 保留（`use-seats-data` + SeatsPanel 内 `buildMemberChatNode`/`buildGroupChatNode` 仍用，作 chat topbar tag 派生）。
- 详情：`specs/prd/version_logs/v0.0.168.ui_opt/change_log.md`（11 条关键用户路径 P1-P11 + IA 决策 D1-D10）+ `specs/ui/version_logs/v0.0.168.ui_opt/change_log.md`（UI 变更总账 + testid 增删表）。

## 2026-07-17 · v0.0.165.ui_upgrade（UI 视觉体系升级：暖橙→银灰 light-only + 8 色 palette 身份点缀 + 坐席面板 + chat-topbar 来源门控返回 + 消息时间 + model picker 样式统一 + 深色模式下线 + idle 严肃化）

- **token 权威源迁移（`[P0]design_system.md` §2/§5.1/§5.3 弃用 → `specs/ui/regulation/01-tokens.md` 银灰体系整表接管）**：tokens.css `:root` 整重写——`--bg=#fafafa` / `--surface=#ffffff` / `--fg=#0a0a0a` / `--btn-primary-bg=#18181b`（黑主 CTA）；新增 8 色 `--hue-{rose,orange,amber,green,teal,blue,violet,pink}` + `-bg` palette（regulation 01 §1.7，仅身份识别用）+ 4 态 `--presence-{online,busy,idle,offline}`（§1.6）+ `--brand-grad`（135° violet→pink→orange 渐变，全站仅 R logo + idle hero orb 两处）+ 4 语义色 `--success/--warning/--danger/--info` + `-bg`（§1.5，替代旧 sage/gold 语义分层）。**双轨命名兼容策略**（R-1 决策）：`--color-*` 前缀 alias 保留但灌新中性值（如 `--color-accent=#18181b`）避免全站 Tailwind `bg-accent`/`text-accent`/`bg-accent-surface` 大爆炸；新代码走无前缀 regulation token（如 `bg-surface`/`text-fg-2`），v0.0.166+ 逐步清 alias。删 `[data-theme=dark]` / `[data-theme=light]` 两作用域块（light-only，regulation 03 §4）+ 删 keyframes `drawerUp/fadeIn/fabIn/floaty/idlePulse/wave`（严肃基调 regulation 03 §3）+ 删 Playfair Display 字体引用（brand 用 `--brand-grad`，regulation 01 §2）。
- **hue-hash 单例（`lib/hue-hash.ts` NEW）**：`hashHueIndex(id:string): 0..7`（djb2 简单串→uint32 % 8，纯函数无依赖）+ `HUE_PALETTE: readonly ['rose','orange','amber','green','teal','blue','violet','pink']`（顺序对齐 regulation 01 §1.7）；`MemberAvatar` / `IconBox` / `SeatCard` / `skill-item` / `icon-box` 场景 hash 派生同 palette 8 色。**INV-5 单一 hash 函数**：全站禁重复实现（reviewer 硬门禁）。
- **`common/component-icon-box.tsx` NEW（regulation 02 §4 primitive）**：22/24/32/34px 方形圆角浅底 icon-box，`hueBy` prop 派生 8 色浅底 + 主色 icon（同实体 hash 同色）；skill / plugin / model / 团队入口图标全走本 primitive。
- **`common/member-avatar.tsx` 改契约（`bgColor` 从 role-based 三色 → hash-by-id 8 色）**：签名加 `id?: string`；leader/mate 分支走 `var(--hue-{palette[hashHueIndex(id ?? name)]})`；user 仍返 `--fg-2`（中性灰）；squad 用 `--brand-grad`（品牌唯一渐变 chip，全站仅 R logo + squad 头像 + idle hero orb 三处）；`showPresence` 新 prop 右下渲 10×10 白 2px 描边点（`--presence-*` 四态）。size='lg' 对齐 regulation 02 §3 =48px。
- **`common/component-model-picker-{panel,trigger}.tsx` NEW（regulation 02 §7 primitives）**：300px 白卡 panel（`radius-lg + shadow-lg + border`，顶部可选搜索 + 可选 headerTitle + 可选 `extraTopItems` + 24px IconBox item + 选中 ✓）+ h32 trigger（`border-2 + radius-md + surface`，22px IconBox + mono 名 + chevron）。**"统一样式不统一数据"**（用户裁决 2026-07-17）：三消费方 `chat/ModelPicker.tsx`（wizard/manage-tab）/ `common/component-key-model-picker.tsx`（app dev config，含搜索、无「默认」项）/ `chat-page/component-input-model-picker.tsx`（chat 输入区，含「a(默认)」置顶 + 完整列表重复出现）内部改组装 primitive；**testid/props 契约完全向后兼容**（消费方零改）。**T4 特例（已批准偏离）**：InputModelPicker **trigger 保留 21px 纯 BrainIcon** 不走 primitive——因 chat-input 3 按钮 21px 统一约束（`action-button-styles.ts`）+ 700 行 UT 全依赖 21px 契约；只 panel 视觉换 primitive，trigger 保留。**panel testid 保留 back-compat**：ModelPicker panel 用 `${triggerTestid}-list`（不改 `-menu`），InputModelPicker 用 `model-picker-menu`/`model-picker-preview`，KeyModelPicker 用 `${suffix}-menu`——三消费方 testid 命名不统一但视觉统一。
- **`chat-page/component-msg-time.tsx` NEW（regulation 02 §6 primitive + INV-7 三页共享）**：`<MsgTime iso side testId?/>`，同日 `HH:mm` / 跨日 `MM-dd HH:mm`（`lib/format-time.ts` 的 `formatMsgTime` 纯函数，now 可注入），10.5px mono `--muted-2`，agent 左对齐 / user 右对齐。`ComponentMessageStream` 在每条消息 bubble 后插入 `<MsgTime>`——一次改覆盖 playground / studio 单聊 / studio 群聊三页（BaseChatPage 统一已在 v0.0.156 完成）。`lib/format-time.ts` 追加 `deriveRelativeTimeKind(iso, now?)` 用于 SeatCard「最近活跃」相对时间派生。
- **`studio-page/component-seats-panel.tsx` + `component-seat-card.tsx` + `component-seat-stats.tsx` + `component-team-entry-row.tsx` + `use-seats-data.ts` + `use-squad-mutations.ts` NEW（坐席面板板块，regulation 02 §9 + PRD §6）**：Studio 主区新 landing view = `MainView {kind:'seats', squadId}`（IA 决策 D7：`selectSquad` 默认落 seats 而非 admin panel，符合 call-center 定位）；页面结构 = 4 格统计条（在线/进行中/今日消息/今日 token，`SeatStats` 组件，null → 显「—」不隐藏格）+ 2 卡团队入口（看板 + 群聊，`TeamEntryRow`）+ 3 列坐席卡网格（`SeatCard` × N，leader 卡 `--border-strong` 描边、offline 卡 opacity 0.75 + primary 降 secondary）。**零后端新增（INV-1/INV-8）**：`use-seats-data.ts` 走 useLifecycle 拉 `listSessions()` 一次 + 派生 `presence` 三态（无 idle：`stateMap[sessionId] ∈ running/interrupting/suspended → busy`；`member.state==='benched' → offline`；else `online`）+ `statusText`（`member.currentWork.text` v0.0.116 已存在，fallback i18n `studio:seats.status.{online,busy,offline}`）+ `lastActiveIso`（`session.updatedAt` 反查）+ budget（`getBudgetUsage(id).consumed` 兜底）；per-member in/out token / 今日消息**无后端字段**→ 降级不显示或「—」。坐席点入对话带 `from:'seats'` 标签（下一条）。`use-squad-mutations.ts` 抽出 mutation handler 簇让 `page-studio.tsx` 从 333→255 行 ≤300。
- **`studio-page/component-chat-topbar-back-btn.tsx` NEW + chat-topbar 来源门控返回按钮**：`MainView.chat` 加 `from?: 'seats' \| 'sidebar'`（缺省 sidebar）；坐席点入传 'seats'，sidebar 点入不传。`StudioChatRouter` 加 `from + onBackToSeats?` 透传；`section-{squad,member}-chat.tsx` topbarLeft slot 消费 `showBackButton=from==='seats'` 门控挂载 `<ChatTopbarBackBtn onClick={onBack}/>` primitive（ghost 型 h32 + ChevronLeftIcon + i18n `common:action.back`——**沿用现有 key 而非新增 `common:back`**，语义等价；section-member-panel 已用同 key）。**INV-6 布局稳定**：back 按钮出现/消失不推挤 title（flex + fixed-width slot）。back 回落到 `{kind:'seats', squadId: node.squadId ?? selectedSquadId}`；无 squadId 兜底 `{kind:'panel'}`。base-chat-page 不动（back 是消费方职责，单一职责）。
- **`chat-page/component-empty-state.tsx` idle hero 严肃化 + BUG-002 视觉还原修复（regulation 02 §1 + 03 §3）**：删 mascot / wave / floaty / idlePulse 三动画 + eyebrow/hint 装饰文案 + radial 环境光；改为**结构化 hero** = brand orb（80×80 `--brand-grad` + 白色 ChatIcon 36px + `--shadow-md`）+ eyebrow（mono 11px muted-2 "Playground"）+ hero-sub（14px muted 400px 副文案）+ CTA h46 primary（黑主按钮 + PlusIcon 16px）+ quick-row（3 chip 分析文件/查资料/写代码，dot 走 `--hue-blue/-green/-violet`）；chip 点击 = 触发 `onNewConversation`（用户裁决：等同主 CTA）。新 testid 6 个 `idle-hero-{orb,eyebrow,sub}` / `idle-quick-row` / `idle-quick-chip-{file,research,code}`；下线 `idle-mascot` / `idle-new-conv-btn`。i18n 新增 5 key（eyebrow/subtitle/chipFile/chipResearch/chipCode）中英对齐；旧 welcomeTitle/welcomeHint 删除。BUG-002 附带修 `primitive-bubble.tsx` user 尖角右下→右上：`rounded-[12px_4px_12px_12px]`（regulation 02 §6 + 设计稿 `.bubble.user { border-top-right-radius: radius-xs }`）。
- **主题下线（`lib/theme-init.ts` → soft_deleted，regulation 03 §4）**：`main.tsx` 删 `initThemeFromConfig` import + 首屏调用；`lib/api-client.ts` 删 `getAppTheme`/`setAppTheme`/`Theme` type；`app-settings-persist.ts` 删 appearance/theme 读写分支（language 仍走同 group `putConfigGroup('app','appearance',[{key:'language'}])`，`change-language.ts` 不动，后端 appearance group 保留）；`use-app-settings-config.ts` 删 `appearance` snapshot/draft/dirty 分支；`app-settings-config-defs.ts` `KV_GROUPS` 移除 appearance 项，`TAB_KV_GROUPS.general = []`（general tab 无 KV group，只挂 `ComponentLocaleCard` 切即生效）；`section-tab-panel.tsx` general tab 只渲染 `<ComponentLocaleCard />`；i18n 新增 `group.locale.label` 中英；旧 `group.appearance.label` / `schema.appearance.theme.*` 保留（back-compat i18n test）。
- **hex 归零（INV-2 硬门禁）**：`grep -rn '#[0-9a-fA-F]\{6\}' app/web/src --include='*.tsx' --include='*.ts' | grep -v __tests__` 从 56 行/28 文件 → 0（除 tokens.css / tests 白名单）；nav-rail brand「R」从 `bg-accent text-white font-serif` → `background: var(--brand-grad)` + `font-sans font-bold`；skill/plugin/model icon-box 从硬编码 hex → hash 派生 `var(--hue-*-bg)` + `var(--hue-*)`；未读红点 `#DC2626` → `var(--danger)`；code review 阶段 grep 归零硬门禁。
- **INV 全清单（reviewer 硬门禁）**：INV-1 后端零改动（`git diff dev1..HEAD -- app/server app/protocols app/shared` 无变化）/ INV-2 hex 归零 / INV-3 无 keyframes（`grep 'animate-\[.*_'` = 0）/ INV-4 Playfair 归零 / INV-5 单 hash 函数 / INV-6 chat-topbar 布局稳定 / INV-7 三 chat 页消息时间同源 / INV-8 坐席卡零后端新增 / INV-9 token 命名双轨兼容。
- **spec 权威源迁移**：`[P0]design_system.md` §2/§5.1/§5.3 弃用（改写为「历史 + 指向 regulation/」），组件视觉规则一律看 `specs/ui/regulation/{01-tokens,02-components,03-principles}.md`；本文件 §3 Tailwind `@theme` 承载机制 + §4 组件词表抽象结构仍适用。

- 详情：`specs/tech/version_logs/v0.0.165/change_plan.md`（9 模块 method 级变更契约）+ `specs/prd/version_logs/v0.0.165.ui_upgrade/change_log.md`（P1-P7 用户路径 + §6.4 字段可得性清单）+ `specs/ui/regulation/{01,02,03}.md`（视觉权威源）。

## 2026-07-16 · v0.0.156（结构性拆分：chat 域 barrel + BaseChatPage 三页统一 + use-chat-actions hook）

- **三 chat 页统一到 BaseChatPage 消费方**：playground（`components/chat-page/section-chat-detail.tsx` 413 → 282 行）重组为 `<BaseChatPage>` + `<BaseChatInputBar>` 消费方，slot 注入 5 处（topbarLeft / topbarRight / messagesSlot / rightOverlaySlot / inputSlot）。ChatDetailProps 签名不变；DOM 锚点（chat-page / chat-topbar / chat-input-bar / chat-send / chat-loading）保留；playground 保留 topbarRight inline（不用 `ComponentChatTopbarRight` 复合，因显隐分支差异）；`hideStopButton` 不传（与群聊 INV-E3 区分，保留 useRunState + stop 按钮）。
- **`base-chat-page.tsx` +1 行 testid**：topbar `<div>` 加 `data-testid="chat-topbar"`——修正 v0.0.155 遗漏（`_overview.md`「沿用的 testid」已含此契约，非新造），保留 playground DOM 锚点（INV-A1-1）。
- **page-chat 抽 `use-chat-actions.ts` hook**：`components/chat-page/use-chat-actions.ts`（新增 260 行）承接 12 handler（openSession / handleModelChange / handleEffortChange / handleApprovalModeChange / handleCreate / handleDelete / handleRenameTitle / handleSend / handleEnqueueCancel / handleCompact / handleClear / handleSelectSub），函数体 100% copy-paste + useCallback deps 数组字面等价（INV-A3-2 防 stale closure：openSession deps 含 `sessions` / handleEffortChange 含 `sessions,setSessions` / handleSend 含 `activeSessionId,model,messages`）；chat-api 9 函数 import 迁到 hook 顶部。`page-chat.tsx` 347 → 210 行变薄（删 12 内联 useCallback + `const actions = useChatActions({...})` + render 内 `actions.handleX` 引用替换 20 处）。
- **chat 域 barrel re-export 拆分（35+14 消费方零改）**：`components/chat-page/types.ts` 522 → 6 行 barrel + 6 子文件（`types/{message,session,hitl,usage,subagent,enqueue}.ts`）；`store/chat-slice-reducer.ts` 495 → 42 行 barrel + 4 子文件（`store/reducer/{agent-event-types,message-preview,reducer-state,apply-agent-event}.ts`，apply-agent-event 324 行超限 24 行——主 reducer 单一职责纯函数不拆 case 违反 INV-G1，barrel header 已留痕）；`lib/chat-api.ts` 403 → 20 行 barrel + 4 子文件（`lib/chat-api/{session-api,message-api,usage-summary-api,workspace-api}.ts`）。
- **`component-message-stream.tsx` 抽 2 avatar**：`components/chat-page/component-message-stream-avatars.tsx`（新增）承接 DefaultAgentAvatar / DefaultUserAvatar；主文件 337 → 310 行，`sideOfMessage` export 保留（studio squad-chat-helpers 消费）。
- **测试 vi.mock 路径补 `.ts` 扩展名（17 文件）**：chat-api 拆成 barrel+子目录后，vitest 下不带 `.ts` 扩展名的 `require('...').resolve(...)` 静默失效——hook 走真实实现导致 postCompact/markSessionRead URL parse 错。批量补扩展名（只改 mock 路径字符串），全绿 196 files / 1879 tests。
- **`[P0]component_architecture.md` 更新**：chat-page 目录结构补 `types/`（6 子域）+ `chat-api/` 子目录、`store/reducer/` 子目录；补三 chat 页统一到 BaseChatPage 消费方 + `use-chat-actions.ts` hook 的架构条目。
- 详情：`specs/tech/version_logs/v0.0.156/change_log.md` + `change_plan.md`（段 A1 / A3 / B / C-1~3 + INV 全清单）。

## 2026-07-14 · v0.0.139（SSE subscribe 钩子体换懒监听新模型）

- **`index.md ④#14 + 表` + `[P0]sse_channel.md §5.1`**：v0.0.85 的「setSubscribeHooks async + await 消除 fire-and-forget 竞争」骨架**不变**；hook 体从旧 `startWatch/stopWatch` 换成懒监听新模型——`onSubscribe` 退化为 no-op（watch 由前端显式 `POST /session/:id/workspace/watch` API 驱动，subscribe 不再隐式建监听）、`onUnsubscribe(session_panel, group)`（1→0）内 `await workspaceManager.recycleSession(sid)` 兜底回收该 session 全部 tab 监听。权威模型见 `../../agent/session/[P0]session_workspace_manager.md §6/§7`。

## 2026-07-14 · v0.0.135（chat 页层次体系：分类法 + z token 标尺 + overlay-root/Portal + footprint gate）

- **z-index token 标尺（`app/web/src/styles/tokens.css` `:root` 块）**：加 4 个 CSS 自定义属性——`--z-base: 0`（L0）/ `--z-floating: 10`（L1）/ `--z-popover: 50`（L2）/ `--z-modal: 1000`（L3，远高于 popover 防串层）。主题无关常量（未给 `[data-theme='dark']` 覆盖）。Tailwind 经 `z-[var(--z-popover)]` 等任意值消费。消灭 chat 页散落的 z-20/z-50/z-60/z-[100]/z-[200] 魔法数字。
- **overlay-root（`app/web/src/lib/overlay-root.ts` NEW）**：`getOverlayRoot()` idempotent 懒创建 `document.body` 下 `<div id="overlay-root">`（`position:absolute; top:0; left:0; width:100%; height:100%; pointer-events:none; z-index:var(--z-modal)`）；cachedRoot 缓存避免反复 DOM 查询；SSR 安全（`typeof document === 'undefined'` 返 null，Portal 容许 target=null）。
- **Portal 极薄 wrapper（`app/web/src/lib/portal.tsx` NEW）**：`<Portal>{children}</Portal>` = `createPortal(children, getOverlayRoot())`，无包装层/无 className；供所有 L3 modal 共用（避免每个 modal 重复 import react-dom）。SSR / 非 DOM 环境返 null。
- **L3 modal `<Portal>` 化（3 modal）**：`component-memory-modal.tsx` / `component-cron-modal.tsx` / `component-clear-confirm-modal.tsx` 顶层 JSX 包 `<Portal>`；根 div `z-[200]`/`z-[100]` → `z-[var(--z-modal)]` + 末尾 `pointer-events-auto`（双保险：overlay-root 容器 none，modal 本体需显式 auto）。脱离 overlay 的 `pointer-events:none` 继承链 → modal 永远可交互、与触发者 DOM 位置无关（结构性修 v0.0.131 modal 不可交互症状）。
- **L1 overlay footprint gate（`component-chat-right-overlay.tsx` + `component-chat-float-menu.tsx` + `component-history-minimap.tsx`）**：overlay 容器 `z-20` → `z-[var(--z-floating)]`；float-menu 插槽 div + minimap 插槽 div 各自**去 `pointer-events-auto`**（留白处穿透 wheel 到 message-stream，结构性修「悬停右缘空白→整会话不滚」症状）；float-menu 本体 + minimap 本体（w-8 列）各自根 div **加 `pointer-events-auto`**（footprint 接事件）。
- **L2 popover z token 化（4 处）**：`component-usage-panel.tsx`（usage-tip + usage-panel 展开浮层，z-50/z-60 → `--z-popover` 同层）+ `component-input-model-picker.tsx`（PICKER_PANEL_CLS + 根 div，z-50 → `--z-popover`）+ `component-mention-popover.tsx`（z-50 → `--z-popover`）+ `component-history-minimap.tsx` preview 气泡（加 `z-[var(--z-popover)]`）。同层不再用 z 区分（视觉前后靠 DOM 顺序 / hover 互斥）。
- **零 API/数据/testid/功能变更**：v0.0.131/133 全部沿用；AT/ET 豁免（纯前端，用户裁决）。3 新增 UT（8 case）覆盖 `getOverlayRoot` idempotent + SSR 安全 + `<Portal>` null 安全。层次体系单一权威 spec：`specs/ui/components/chat-page/_layering.md`（architect 产出，4 槽分类法 + z 标尺 + 两条 invariant + 归位映射）。详情：`specs/tech/version_logs/v0.0.135/change_plan.md` + `specs/prd/version_logs/v0.0.135/change_log.md`。

## 2026-07-13 · v0.0.133（会话区布局修复：overlay 定位模型 + wrapper 定位上下文 + break-words）

- **右缘 overlay 定位模型变更（`component-chat-right-overlay.tsx`）**：根 div 由 `absolute right-3 top-16 ... gap-3`（堆顶）→ `absolute inset-y-0 right-3 z-20 flex flex-col items-end pointer-events-none`（纵向铺满定位上下文）。内部纵向分配重写：float-menu（`children`）包裹 `pt-3 pointer-events-auto`（贴顶 + topbar 留距）；minimap 包裹 `flex-1 flex items-center justify-end min-h-0 pointer-events-auto`（占满剩余纵向空间 + **纵向居中**，修 v0.0.131 minimap 堆顶问题 F1）。overlay 仍 absolute 悬浮（用户硬约束，不做实体 gutter 列）。
- **消息区 wrapper = overlay 定位上下文（3 root 共享结构）**：`section-chat-detail.tsx` / `section-member-chat.tsx` / `section-squad-chat.tsx` 把 `{empty 占位 / ComponentMessageStream} + {sessionId && ComponentChatRightOverlay}` 同包进一层 `<div className="flex-1 flex flex-col relative min-h-0 min-w-0 overflow-hidden">`（input-bar 留 wrapper 外）。wrapper 的 `relative` = overlay 定位基准（`inset-y-0` 指 wrapper 高 = 消息区高度）；`overflow-hidden` 杀横向滚动（F3）；`min-w-0/min-h-0` 贯通 flex 链。定位上下文从 v0.0.131 的「section 根」改为「消息区 wrapper」。
- **消息区右侧 reserve（`component-message-stream.tsx`）**：根 scroll div `px-8` → `pl-8 pr-[80px]`——右侧 80px 预留悬浮 overlay 区，居中正文（`max-w-[820px] mx-auto`）左移、右侧 user 头像让位（F2）。三处 chat root 共享同一 ComponentMessageStream，reserve 一致。
- **长 token 换行（`primitive-bubble.tsx` + `primitive-markdown-view.tsx`，F4）**：PrimitiveBubble user/assistant 两态 className 加 `break-words`（长 token / URL 在 `max-w-full` 内换行不撑宽）；PrimitiveMarkdownView root `<div>` 加 `break-words min-w-0`（`min-w-0` 让 root 在 flex 链内可缩）+ 段落 `<p>` 加 `break-words`。
- **零 API/数据/testid/功能变更**：v0.0.131 全部沿用；AT/ET 豁免（纯前端，用户裁决）。详情：`specs/tech/version_logs/v0.0.133/change_plan.md` + `specs/prd/version_logs/v0.0.133/change_log.md`。

## 2026-07-13 · v0.0.131（会话区 2 升级：历史 query minimap + 右上悬浮菜单）

- **flatten 单次分发（`use-flattened-view.ts` + `component-message-stream.tsx`）**：三处 chat root 用 `useFlattenedView(messages, opts)` 单次 flatten → 分发 message-stream（新增可选 `flattened` prop，fallback 内部计算，零回归）+ minimap；`deriveMinimapBars`（`minimap-bars.ts`）从同一份 `elements` 派生 bar，保证「bar 数 = 可见右侧 user 气泡数」恒等（不二次 flatten）。
- **历史 query minimap（`component-history-minimap.tsx`）**：右缘竖排 bar + Dock 悬停放大（CSS width transition）+ 左侧预览气泡 + 点击 scrollIntoView 跳转。bar = **按 side 判定的右侧 user 气泡**（非裸 kind）——修正「a2a inbox 不产 user-text」的误解（a2a `role:'user'` 确产 user-text，靠 `sideResolver ?? sideOfMessage` 判定排除群聊左侧 a2a）。
- **右上悬浮菜单 + 弹层二级视图（`component-chat-float-menu.tsx` + `component-chat-right-overlay.tsx`）**：收纳原 ws-panel memory/cron tab → 悬浮工具条 + badge（记忆数 / active cron 数，=0 隐藏）；点项开弹层（`component-memory-modal`/`component-cron-modal`），弹层内 list↔editor 二级视图 + 返回按钮（不弹层套 modal）。badge 与弹层同源（float-menu 恒挂载 `useMemoryCrud` + 新建 `useCronCrud`，chat 挂载即取；用户 CRUD 写后 refetch 即时更新，agent 侧写入非实时 known-boundary）。
- **ws-panel 收敛 tab（废弃删除）**：`component-ws-tab-bar` 删 memory/cron tab（`ws-tab-memory`/`cron-tab` testid 退役）+ `hideCronTab` prop；`section-workspace-panel` 删 memory/cron 分支 + activeTab state；`section-right-tabs` 删 `showCronTab`；删 `section-memory-panel.tsx`/`section-cron-panel.tsx`（内容迁弹层，`useCronCrud` 抽出 + 卡片/表单零件复用）。
- **memory 表单抽出（`component-memory-editor-fields.tsx`）**：从 `component-memory-editor-modal` 抽纯表单字段，弹层二级视图与 app config 独立 modal 共用（DRY，testid 前缀不变）。
- 详情：`specs/tech/version_logs/v0.0.131/change_plan.md`

## 2026-07-11 · v0.0.113（studio member 面板 skills overlay 重构 + 记忆组件删除）

- **`[P0]component_architecture.md §3.1 + 文件树`**：`studio-page/` 组件清单更新——删 `component-member-panel-memory.tsx`（记忆 section 移除，记忆入口迁 chat 右侧 `section-right-tabs`）+ 加 `component-member-skill-filter.tsx`（成员 skills 简化筛选器）；`section-member-panel.tsx` 改 skills overlay switch。squad 侧 skillConfig/overlay resolve 权威见 `specs/tech/squad/`。
- 详情：`specs/tech/version_logs/v0.0.113/change_log.md`

## 2026-07-09 · v0.0.97（enqueue 队列重构：纯 API(GET /inbox)+SSE 驱动 + cancel 转圈 + mention pill）

- **useMessages onInit 双 GET seed（`use-messages.ts`）**：onInit 在 subscribe（agent_loop + session_panel）之后追加 GET /messages → GET /inbox seed enqueueItems。**subscribe-first（D8 权威）**：subscribe 必须在 GET 之前（GET 返回到 subscribe 间 fire 的 message_enqueued 既不在 GET 快照又没订阅到会丢）；GET 与 SSE 重叠条目靠 reducer `some(enqueueId)` 幂等去重。GET /inbox 失败降级空不阻塞主对话流。content 经 `contentBlocksToPreviewText`（store/chat-slice-reducer.ts:75）拍平 ContentBlock[] → string。
- **删 addEnqueueItem / removeEnqueueItem 命令式方法（dead code）**：队列加项只由 SSE `message_enqueued` 驱动（onInit GET seed + reducer），移项只由 SSE `enqueued_message_canceled`/`processed` 驱动（INV-1/INV-5 多端一致性）。POST /messages 响应、cancel POST 响应都不进 reducer。caller（page-chat / section-member-chat）同步删调用。
- **component-enqueue-view cancel 转圈（`component-enqueue-view.tsx`）**：点 x → 本地 `canceling: Set<enqueueId>` 加项 + POST cancel（fire-and-forget）+ 1s 后回 x（cancel POST 幂等可重试）；转圈期 `disabled` 禁点防重复 POST；队列移项靠 SSE `enqueued_message_canceled`（不进 store，INV-3）。spinner = inline span border+animate-spin（码内约定非 SVG，abort-btn 同款），占同 22×22 槽位（INV-4）。unmount 时 useEffect 清 pending timer 防 React warn。cancel 态 DOM 断言用 `data-canceling` 属性（不加新 testid）。
- **component-enqueue-view mention pill**：content 经 `toTextPreview` 产预览 string 后，经 `<MentionRender text={preview}/>` 渲染——解析内联 `<mention/>` tag 为 pill（复用 component-mention-render.tsx），无 mention 降级纯文本。删 `onRemove` prop（run-state-bar 从未传，死代码）。
- 引用：`[P0]chat_area_hooks.md §3`（onInit subscribe-first + 双 GET seed + 删命令式方法）+ `chat-page/_overview.md §4.11a` + `chat-page/_components.md` + `studio-page/member-chat-page.md`。详情 `specs/tech/version_logs/v0.0.97/change_log.md`。

## 2026-07-09 · v0.0.96.ui_fix（UI/架构修复三件套：usage 格式 + spinner 双源兜底 + studio chat 自给）

- **formatTraffic util 新增（`app/web/src/lib/format-traffic.ts`）**：纯函数 `formatTraffic(n)` 按 K/M/B/T 量级分级（< 1000 原值；K=1e3；M=1e6；B=1e9；T=1e12，均 1 位小数 toFixed 自带 round）；边界 0/负/NaN/Infinity → `String(n)` 防 `-1.5K`/`NaNK` 丑态。仅用于累积消耗表格 input/output/total 三列（O1 裁决：概览「已用/总」fmtK 不动，cache 列百分比不动）。`component-usage-panel.tsx` CumCells 改调 formatTraffic + cum-table grid-template-columns 从 `1fr auto×4` → `84px_minmax(48px,1fr)×4`（标签列固定 84px 防过短 + 值列 minmax 防折行）+ 值列 `tabular-nums`（等宽数字防 K→M 切换左右跳）。spec：`specs/ui/components/chat-page/component-usage-panel.md §4.3/§4.7` v1.2。
- **spinner 双源门控（`component-message-stream.tsx:292`）**：on-message spinner 渲染条件从 `{runActive && ...}` 扩为 `{(runActive || sessionRunning) && ...}`（双源或运算）。主源 runActive（agent_loop run_start→true / run_end→false）；兜底 sessionRunning（GET /session REST + session_panel session_status_update 同步）覆盖两类 sticky replay 失效场景：**切会话**（ComponentMessageStream remount，runActive 重 init=false，session_meta 到达前 agent_loop sticky 槽未恢复窗口）+ **SSE 重连**（断线重连后 sticky slot 不可恢复）。phase 文案仍只跟 loadingPhase（agent_loop 派生）；sessionRunning 触发窗口期 loadingPhase=null → 兜底 'thinking'。**父组件零改**：ComponentMessageStream 早已收双 prop（page-chat/member-chat 均传 runActive + sessionRunning），仅组件内 spinner 门控扩或运算；ComponentToolBatch 仍用精确 runActive 不污染。spec：`chat-page/_overview.md §4.10`（双源兜底明化）。
- **useStudioChatChrome 新增（`app/web/src/components/studio-page/use-studio-chat-chrome.ts`）**：Studio chat 自给 chrome hook——`useStudioChatChrome(sessionId) → { chrome: StudioChatChrome | null, loading, error }`。走 useLifecycle 四方法契约（genRef 守卫 + signal.aborted + deps:[sessionId] 重订阅）但**不订 SSE**（纯 GET-once）；onInit 流程：`getSession(sessionId)` → `s.squadId`/`s.memberId`（API spec 11-squad §2 已声明，前端 chat-page/types.ts v0.0.96 补字段对齐）→ `signal.aborted` 校验 → `squadId` 缺失 throw（走 error 通道）→ `getSquad(s.squadId)` → `signal.aborted` 校验 → 派生 member（单聊 squad.members.find / 群聊 undefined）→ return `{ member, squad, squadModelDefault, tag, isGroup }`。**失败一律 throw → error 通道**（sessionId 空 / squadId 缺失 / network fail），useLifecycle 类型不许 null。spec：`[P0]component_data_map.md §6`（首次为非 SSE 的 chat 自给 hook 落契约）。
- **router 脱 detail + MemberChatPage/SquadChatPage props 收敛**：`component-studio-chat-router.tsx` 删 `detail` prop + 自调 useStudioChatChrome(node.sessionId) 拿 `chrome.isGroup` 决定挂 SquadChatPage/MemberChatPage（消除旧 detail 门控 + MemberChatPage↔SquadChatPage 抖动 race——切 squad 时 detail async 窗口内旧 detail 不匹配的视图类型反复跳）；`section-member-chat.tsx` props 删 `member`/`squadId`/`squadModelDefault`/`tag`（留 sessionId/onOpenMember/prefill）+ 拆 wrapper/inner（wrapper 管 chrome loading 门控渲 chat-loading 占位，inner 内 hooks 无条件执行满足 React 规则）；`section-squad-chat.tsx` props 删 `squadModelDefault`/`tag`/`title`（留 sessionId/prefill）+ 拆 wrapper/inner；`page-studio.tsx` 删 `detail={detail}` 透传。三处 wrapper（router/member/squad）chrome loading/error 期间统一渲 `<div data-testid="chat-loading">…</div>` 占位（不 mount area-hooks 避免无 sessionId 残留；error 不自管 UI，dev console warn 已在 hook 落）。**取舍（方案 A）**：chat 自查重复拉一份 GET /squad（router + Page 各调 chrome = 2× GET/session + 2× GET/squad，O3 YAGNI 接受，本地 dev < 50ms），换彻底解耦。spec：`studio-page/_overview.md §4b` + `member-chat-page.md`/`squad-chat-page.md` Props v2.6。
- **API spec 对齐（`04-agent-session.md §2.1`）**：Session interface 补 `squadId?: string` + `memberId?: string`（11-squad §2 自 v0.0.33.1 已声明，04 漏定义——v0.0.56 重构遗留；前端 types.ts 已补，本版 doc-sync 补 API spec 对齐）。
- 引用：`[P0]component_data_map.md §6` + `chat-page/_overview.md §4.10` + `studio-page/_overview.md §4b` + `component-usage-panel.md §4.3/§4.7` + `04-agent-session.md §2.1` + `reqs/[working] v0.0.96.ui_fix/req.md`

详情：`specs/tech/version_logs/v0.0.96.ui_fix/change_log.md`

## 2026-07-08 · v0.0.95.lifecycle_buffer（useLifecycle 加 buffer 第三参数，消灭 useMessages 流式特例）

- **buffer 第三参数落地（`[P0]component_architecture.md §3.10`）**：`app/web/src/lib/use-lifecycle.ts` 加 TBuffer 泛型 + `bufferRef` + `commitBuffer`（同步写 ref 不 setCtx 不渲染）；四方法签名加 buffer——onInit 返 `{ctx,buffer}`（兼容旧 ctx-only 返回，hook 内部包装）；onEvent/onTick 收 `(ctx,buffer,event,from)` 返 `{ctx?,buffer?}`（buffer 放最后保向后兼容）；onDestroy 收 `(ctx, buffer|null)`。命令式口子 mutate 分裂：新增 `mutateCtx`（走 commitCtx 触发渲染）+ `mutateBuffer`（走 commitBuffer 不渲染），保 `mutate` 作 `mutateCtx` 别名供 4 个 v0.0.94 消费者（use-run-state/use-page-chat-mount/use-session-panel-fanout 等）零改动。新增**不变量⑦ buffer 变不渲染** + **不变量⑧ onEvent 串行调度**（handleFrame 同步调 onEvent 后立即 commit，一帧处理完才接下一帧，SseClient 单线程顺序投递无重入）。
- **reducer 纯化（`app/web/src/store/chat-slice-reducer.ts`）**：`applyAgentEventToMessages` 从 `(msgs, evt, ctxRef:{current: RunContext|null}, state)` mutate → `(msgs, runCtx: RunContext|null, evt, state) => ReducerFullResult` 纯函数（新增类型 `ReducerFullResult = ReducerResult & { runCtx: RunContext | null }`）；6 个 `ctxRef.current` 副作用点全改 immutable return——run_start 新建 runCtx；tool_call_delta 新 Map 累积（拷贝旧 entries + set/累积 toolCallId 对应 raw）；tool_call_end 读 rawMap 拿累积值 parse 进 ctx.messages + 返**删了该 key 的新 Map**（D2 落地：reducer 内清理 buffer，跨帧不残留半截 rawArgs）；error/run_end 各自新对象/返 null。`chat-slice.ts` 增加 re-export `ReducerFullResult`。
- **useMessages 进契约（消灭特例，`[P0]chat_area_hooks.md §3`）**：删自管 `sliceRef`/`runCtxRef`/`setSlice`/`hasMoreRef`/`setHasMore`/`genRef`；ctx=`{messages,hasMore,runActive,loadingPhase,lastRunFinish,enqueueItems}` 走渲染通道（onEvent 返 ctx 字段触发 commitCtx+setCtx），buffer=`{runCtx:RunContext|null}` 走工作内存通道（onEvent 返 buffer 字段只 commitBuffer 不渲染）。onInit GET /messages(limit 50) → return `{ctx: initialCtx, buffer:{runCtx:null}}` + 声明 `subscribe(agent_loop)` + `subscribe(session_panel)`。onEvent `agent_loop` 分支调纯化 reducer 返 `{ctx:新, buffer:{runCtx:新}}`（双写）；`session_panel messages_cleared` 清 ctx；终态孤儿清理保留（D7）。命令式 `setMessages`/`removeEnqueueItem` 改走 `mutateCtx`。
- **验证**：UT 全绿（含新增 buffer 不渲染 / 串行调度 / buffer 清理 / ctx+buffer 同步累积 4 组 UT）。ET 流式核心路径（squad_no_polling / studio_member_messages_render）PASS 证明 reducer 纯化不回归流式语义（不丢字、rawArgs 累积正确）；chat_basic Tiptap UI send case 是既有 BUG-001 债（非本版回归）。
- 引用：`[P0]component_architecture.md §3.10` + `[P0]chat_area_hooks.md §3` + `[P0]lifecycle_data_shapes.md §3.2` + `reqs/[done] v0.0.95.lifecycle_buffer/req.md`

详情：`specs/tech/version_logs/v0.0.95.lifecycle_buffer/change_plan.md`

## 2026-07-08 · v0.0.94.component_refactor（useLifecycle 四方法契约 + 数据三形 + 对话区引擎拆解，全组件迁移）

- **useLifecycle 四方法 + mutate 口子（`[P0]component_architecture.md §3.10`）**：v0.0.92 三方法 `init/destroy/refresh+poll(config)` → 四方法 `onInit/onDestroy/onTick/onEvent` + `LifecycleInitApi{signal,startTimer,subscribe}`（onInit 内声明式）。新增**不变量① ctx ref-latest**（onEvent/onTick/mutate 收 ctxRef.current 最新非 React 快照，返回值同步写 ref + 排队 setCtx——治 agent_loop 高频流帧覆盖丢字，把手写 sliceRef 升为契约默认）。effect 声明式订阅/timer 自动回收归 useLifecycle。控制模型：useLifecycle 攥 ctxRef+setCtx 不外发，**两个命令式口子**（design-decisions §5）：`reload()` re-init 重订阅/起 timer；`mutate(updater)` 局部改 ctx 走同一 ref-latest 写回路径（commitCtx），不重订阅/不重 init，消灭 overlay useState/ref+reload workaround（数据分裂违背原子化）。
- **数据三形标准化（`[P0]lifecycle_data_shapes.md` NEW）**：`app/web/src/lib/lifecycle-shapes.ts` 定义 `Collection<T>`(applyCrud upsert/delete/replace) / `Snapshot<T>`(applySnapshot replace/patch) / `KeyedMap<K,V>`(applyKeyed set/delete/clear) 三形 + 纯 reducer（immutable + 幂等无变化返原引用）。每 hook 恰好持一形一块数据。**流式特例** useMessages 保留 `applyAgentEventToMessages` 领域 reducer 不套三形（v0.0.95 buffer 第三参数已消灭此特例，reducer 纯化进契约，见下方 v0.0.95 条目）。**KeyedMap 消费方 ctx 须显式 `KeyedMap<string,V>`**（K 从字面量反推会跨 hook 类型不兼容，§2.3）。
- **对话区引擎拆解（`[P0]chat_area_hooks.md` NEW）**：v0.0.39 `useSessionRunState` monolith（订双 topic 揽 5 区域）拆成 area-hooks：`useMessages`(agent_loop 流+session_panel messages_cleared/status 终态清孤儿)/`useRunState`(session_panel status_update+agent_loop run_end 校正)/`useUsage`(usage_update)/`useSummary`(summary_task_update)/`useSessionPanelFanout`(workspace/read 扇出到 store)。三页(page-chat/MemberChatPage/SquadChatPage) compose 同源 area-hooks。删 `use-session-run-state.ts`+`use-session-sse-subscribe.ts`。状态自愈跨 topic 归属明确（run_end 校正归 useRunState、sticky 孤儿清理归 useMessages），不引入跨 hook 回调。
- **组件-数据源拆解表独立成文件（OP4 架构 hygiene）**：原 §3.11 全组件迁移映射表（18 个数据 hook/组件 → 四方法契约映射）下沉到独立 `[P0]component_data_map.md`（70 行，完整 OKF frontmatter）——是「组件-数据源拆解标准」永久落地，后续版本增删组件须同步更新。§3.11 改成指针。**pre-coding 硬门禁**：涉 UI/数据需求进编码前必须先出组件-数据源拆解表（按本表结构填新组件，memory `ui-req-needs-component-datasource-decomposition`）。`component_architecture.md` 从 317 → 299 行（≤300 通过）。
- **全组件迁移内容**：18 个数据 hook/组件映射。已迁 4 hook（useMemoryCrud/MemberPanelMemory/SectionWorkspacePanel/useSubagentChildren）re-fit 新 API。usePageChatMount/useStudioUnreadMeta/StudioSidebar/SquadBoard 新迁。**useStudioUnreadMeta 修 G1 违规**（沿用 v0.0.92 已改单例，本版套契约）。
- **SSE 优先核实（design-decisions §6，spec↔code 漂移已汇报）**：`BudgetMeter` 核实 `session_usage_update` 携 per-session `SessionUsageView`（非 squad `BudgetUsage`），**无现成 topic** → 保 onTick 30s poll 兜底（design-decisions §6 premise「已存在只是没接线」与代码不符）。`SectionCronPanel`(60s)/`PageConnector`(5s) 同无 topic → onTick 兜底。三者走 `effect.startTimer` 标准化 + justification，禁裸 setInterval。

## 2026-07-08 · v0.0.92.sse_opt（SSE 重连 + useStudioUnreadMeta 单例收敛 + useLifecycle hook 抽象）

- **G1 修复**（W1）：`use-studio-unread-meta.ts` 改用 `getSseClient()` 单例订阅 `session_meta _all` + subId 区分 handler；删 `new SseClient()` / `void sse.connect()` / `sse.destroy()`；unmount 仅 unsubscribe 句柄。`[P0]sse_client_singleton.md §5` R3 注脚修正（标 since v0.0.92 已收敛）。
- **SSE 重连（R2/W2）**：`SseClient` 加指数回退自动重连（base 1s × 2^k cap 30s + jitter ±20%）；reader.read catch 触发，handlers Map 不清；`onResumed(cb)` 注册「重连成功 / visibility 返前台」回调；singleton 顶层注册 visibilitychange listener；destroyed 守卫防僵尸重连。`[P0]sse_client_singleton.md §7` 标「（可选）」→「since v0.0.92 已落地」。
- **轮询清理 + dead code 删除（W3）**：(a) `component-conversation-item.tsx` 删 pollRef/stopPolling dead code（v0.0.88 轮询消除后残留，从未赋值）；(b) `component-mention-popover.tsx` debounce 已有 cleanup（G6 研究描述错误，无改动）；(c) `component-budget-meter.tsx` 30s 保留（squad-level SSE 改造 out-of-scope）+ 后台 tab 暂停 + 切回立即 refresh；(d) `page-connector.tsx` 2s→5s + 后台暂停；(e) `section-cron-panel.tsx` 60s 保留 + justification。
- **useLifecycle hook 抽象（W4 核心）**：新建 `app/web/src/lib/use-lifecycle.ts` —— `init/destroy/refresh + poll.justification + deps` 契约；5 不变量（init 接 AbortSignal / destroy 幂等 / poll 显式+justification / 禁 init 内 setState 除 ctx / 禁 destroy 内 fetch）+ 6 禁忌（禁裸 setInterval / 禁裸 setTimeout / 禁 new SseClient 等）；与 getSseClient() 单例兼容（init 内 subscribe / destroy 内 unsubscribe，不动连接）。`[P0]component_architecture.md` 新增 §3.10 useLifecycle 契约。
- **reload-on-resume poll-only 契约（code-review 修复）**：useLifecycle 的 visibilitychange 在 tab 切回 visible 时**仅当启用 poll 才 reload**（非 poll hook 不重载——数据 holder 走订阅/SSE onResumed 刷新、命令式 API 不在 tab 切换重载）。根因：useSubagentChildren 借 useLifecycle 只为用其 unmount cleanup（abort in-flight），不传 poll；原 onVisibility 无条件 reload → destroy 清 controllersRef → 切回 tab 静默丢 in-flight 请求（code-review 抓出 Major 后定）。`use-lifecycle.ts:198-209 onVisibility`。`[P0]component_architecture.md §3.10 轮询策略` 已记为正式契约。
- **全迁 4 hook（W5+T6，从原试点 2 个扩为非引擎非轮询全迁）**：`use-memory-crud.ts`（W5）+ `component-member-panel-memory.tsx`（W5，删 setTimeout reload 改 reload() 命令式 + feedback setTimeout 加 unmount cleanup）+ `section-workspace-panel.tsx`（W6，顶层 tree GET 走 useLifecycle）+ `use-subagent-children.ts`（T6，借 useLifecycle unmount cleanup abort in-flight）；剩余轮询类 hook（BudgetMeter/CronPanel/Connector）记 follow-up。
- **偏离记录**：(1) sse_lifecycle_audit §1.2 G6 称 mention-popover debounce 未 clear，实际 line 132-140 已有 cleanup（doc-modifier 阶段 5 同步修研究文档）；(2) budget-meter 改 SSE 不可行（session_usage_update 是 per-session 事件，非 squad 级广播），squad-level SSE 改造记 follow-up。

详情：`specs/tech/version_logs/v0.0.92.sse_opt/change_plan.md`

## 2026-07-07 · v0.0.85.ui_opt（sse_channel setSubscribeHooks async + session_workspace_manager await ready/addDir）

- **`[P0]sse_channel.md §5` setSubscribeHooks 接口 async**（F2，消除 fire-and-forget 竞争）：`SubscribeHooks.onSubscribe/onUnsubscribe` 返回类型 `void → void | Promise<void>`（保留 sync 兼容）；`SseChannel.subscribe/unsubscribe` 内 `await hooks.onSubscribe?.(...)` / `await hooks.onUnsubscribe?.(...)`（hook 异常 try/catch 不影响订阅本身）。
- **配合 session_workspace_manager `startWatch` await chokidar 'ready' + addDir listener**（F2 核心修复，详见 `specs/tech/agent/session/log.md` v0.0.85.ui_opt 条目）：bootstrap `setSubscribeHooks` 改 async 函数 + 内部 `await workspaceManager.startWatch/stopWatch`，消除原 `void startWatch/stopWatch` 的快速切 tab 时序残留/漏启。
- **前端订阅覆盖 squad 群聊**（F2 D1 修复）：section-squad-chat.tsx 新增独立 SseClient + connect + subscribe session_panel handler（仅处理 workspace_file_changed / dir_changed → setLastWorkspaceEvent）；section-member-chat.tsx 补一行 onWorkspaceEvent 接 store.setLastWorkspaceEvent（修当前漏接）。SseChannel 后端 subs Map 单订阅去重 + 多 listener fan-out 保证多 SseClient 实例都收到同一 SSE 帧（spec §5/§7）。

详情：`specs/tech/version_logs/v0.0.85.ui_opt/change_plan.md`

## 2026-07-01 · v0.0.42

- **块 1（依赖后端）**：`[P0]sse_channel.md` §10.7 加「replay 粘住 run 生命周期标记」——agent_loop bus 注入 `lifecyclePredicate`，让 `run_start`/`run_end` 额外写入 sticky slot（与 content buffer 分离），`clearReplay` 只清 content、不清 sticky；切走切回重订阅时 sticky 先回放 → reducer runActive 恢复 → spinner 回归。详见 `../../agent/event/[P0]event_bus.md §4.3`。
- **块 2 两层状态分离**：`[P0]sse_channel.md` §9.1 加「session vs run/message 两层严格分离」表（session 层 GET /session + session_panel 驱动 stop 按钮；run 层 agent_loop replay 驱动 on-message spinner）。移除原 §4.10 浮动 loading 胶囊（`absolute left-10 bottom-[72px]`）。
- **块 2 组件契约**：`[P0]component_architecture.md` §3.7 加 stop 按钮圆环视觉（外圈旋转环 + 中心实心方框，interrupting 减速）+ on-message spinner（贴流式尾部，spinner+phase 同控件状态各自决定）+ ComponentRunStateBar 组装变化（移除 loading 胶囊引用）+ 引擎恢复路径。
- **块 3 sideResolver prop**：`[P0]component_architecture.md` §3.6 加 `component-message-stream` 可选 `sideResolver?: (msg) => 'user'|'assistant'`（单一职责，与 resolveActor 解耦；采纳独立 prop，否决 actor.side 字段）。三视图策略：单聊 a2a→右、群聊默认、playground 默认零回归。
- 影响代码：`component-abort-btn.tsx`（圆环改造）+ `component-loading-status.tsx`（移除浮动改 on-message spinner）+ `component-message-stream.tsx`（加 sideResolver + 末尾 spinner）+ `component-run-state-bar.tsx`（移除 loading 胶囊）+ `section-chat-detail.tsx`（装配）+ `section-member-chat.tsx` + `section-squad-chat.tsx`（IME 守护）。`useSessionRunState` hook 零改动。
- 后端配套：`app/server/src/agent/event-bus.ts`（lifecyclePredicate + sticky slot）+ `bootstrap.ts`（agent_loop bus 注入 predicate）。

详情：`specs/tech/version_logs/v0.0.42/change_log.md`

## 2026-06-30 · v0.0.39

- squad 对话 UI 重写：`component-message-stream.tsx` 参数化为共享渲染内核 `ComponentMessageStream`（props：`resolveActor`/`messageFilter`/`blockFilter`/`sideOfMessage` + 可选 `runActive`/`sessionRunning`/`lastRunFinish`）；playground/单聊/群聊三视图复用同一内核。
- 单聊/群聊拆 `section-member-chat.tsx` + `section-squad-chat.tsx`，共用 `squad-chat-helpers.tsx`（`isUser`/`isA2aInbox`/`sideOfMessage` 谓词）；新增 `common/member-avatar.tsx`（色块 + 首字母，对端 member 头像）。
- 可见性策略：单聊全展示（仅滤 reminder）/ 群聊白名单（`isUser||isA2aInbox`，mute assistant answer + tool + reminder）；a2a inbox 归 assistant 侧（`sideOfMessage`，即便后端存 `role='user'`）。
- `[P0]component_architecture.md` §3.3 补「共享渲染内核 ChatStream」；§3.1/§3.2 studio page 加 v0.0.39 重写注记。
- playground 零回归（新 prop 全可选、不传策略 hook 时默认 Rocky/U）。

### Round 2（共享 run 态引擎 + store 瘦身 + P1 bizType 守卫）

- 共享 run 态引擎 `useSessionRunState(sessionId, opts?)`（`chat-page/use-session-run-state.ts`，282 行）：playground `page-chat` + studio 单聊 `section-member-chat` 共用——SSE `agent_loop`/`session_panel` → 同一套纯 reducer（`applyAgentEventToMessages` + `applySessionStatusUpdate`）→ messages/runActive/loadingPhase/lastRunFinish/sessionRunning/enqueueItems/usage/summaryTask + abort/reset。**不写第三套 reducer**。
- store 瘦身：`chat-slice.ts` run-state 切片整体迁出（messages/runActive/loadingPhase/lastRunFinish/sessionRunning/enqueueItems/usage/summaryTask 及 actions），只留 sessions[] 列表 + activeSessionId + subagent tree（childrenByParent/activeSubId）+ lastWorkspaceEvent + applySessionMetaEvent。
- P1 bizType 列表隔离：`chat-slice.ts.applySessionMetaEvent()` 加守卫 `if (incoming.bizType === 'studio') return;`——session_meta 是 `_all` 共享广播，studio 会话 running 时广播会泄漏进 playground 列表，守卫挡掉。前端 `Session` 类型（`chat-page/types.ts`）补 `bizType?: 'playground' | 'studio'`。
- SSE 所有权（对 playground 零回归）：playground 注入 page-chat 的 `sharedSse`（模块级单例，hook 只 subscribe/unsubscribe **不 destroy**）；单聊省略 sseClient → hook 内部 new + connect，cleanup 时 unsubscribe + destroy（不碰 playground sharedSse）。
- 瞬态修复：切 session reset 落 `useLayoutEffect`（paint 前同步清空 messages/runActive/sessionRunning/usage/summaryTask），恢复 v0.0.30「切会话不留旧 messages 残留」行为（迁到 hook 后 reset 落 post-paint `useEffect` 被削弱，此处补回时序）。
- `[P0]component_architecture.md` §3.4 补「共享 run 态引擎 useSessionRunState」（接口签名 + SSE 订阅契约 + 与 store 的关系 + 代码路径 + subagent transcript 补全）。
- index.md ① 概念表加「共享 run 态引擎」行 + session_meta 行补 bizType 守卫注记；④ 加核心原则 8（run 态从 store 迁 hook）。

### Round 3（共享 run 态 UI 组装层 + 补接单聊 enqueue 排队区）

- **触发**：用户发现 studio 单聊（`section-member-chat.tsx`）run 态 UI 只手写接了 loading 胶囊 + 停止按钮，漏了 enqueue 排队区（Round 2 引擎已算好 `enqueueItems`/`removeEnqueueItem`，只是没人接 UI）。
- 新增共享组件 `chat-page/component-run-state-bar.tsx`（两导出）：`ComponentRunStateBar`（Fragment）= loading 胶囊（`ComponentLoadingStatus`，`phase=runActive?loadingPhase:null`）+ enqueue 排队区（`ComponentEnqueueView`，`showEnqueue` 门控，playground readOnly 传 `false` 仅留 loading）；`ComponentRunStateAbortSlot` = 停止按钮「`sessionRunning && sessionId` 才渲染」的判断收拢（位置仍由 caller 内联在自己 input-row，两边 send 布局不同不抽）。是继渲染内核 ComponentMessageStream（Round 1）、run 态引擎 useSessionRunState（Round 2）之后的**第三个共享件**——解决"引擎数据→UI 组件"组装层此前由各消费方手写、容易漏字段的问题。
- playground（`section-chat-detail.tsx`）**真迁移**：删旧三个叶子组件（`ComponentLoadingStatus`/`ComponentEnqueueView`/`ComponentAbortBtn`）的直接 import，全切 `ComponentRunStateBar`/`ComponentRunStateAbortSlot`，无并存；readOnly 分支 `showEnqueue=false` 仅留 loading 胶囊，行为不变。
- studio 单聊（`section-member-chat.tsx`）补接 `enqueueItems`/`removeEnqueueItem` + 本地 `handleEnqueueCancel`（乐观移除 + `cancelEnqueue` POST，仿 playground `page-chat` 写法），换装同一控制条，补回此前漏接的排队区。
- 零回归：渲为 Fragment 不引入新定位上下文，loading 胶囊 `absolute` 物理位置不变（wrapper 满宽贴底，左下角=页根左下角），testid 全保留（`chat-loading-status`/`enqueue-view`/`chat-abort`，叶子组件自带不变）。
- `[P0]component_architecture.md` §3.5 补「共享 run 态 UI 组装层 ComponentRunStateBar」（核心概念 + 设计思路 + 零回归要点 + 代码路径）；`specs/ui/components/chat-page/_components.md` + `studio-page/member-chat-page.md` 同步。
- index.md ① 概念表加「共享 run 态 UI 组装层」行；④ 加核心原则 9（共享 UI 组装层，引擎数据与渲染解耦）。
- code-review：✅ PASSED，0 Critical/Major/Minor。

详情：`specs/tech/version_logs/v0.0.39/change_log.md`

## 2026-06-30 · v0.0.35

- OKF KB 化：建 `index.md`（5 章总起）+ 本 `log.md`。
- `[P0]component_architecture.md` 补 YAML frontmatter（之前缺失）；删顶部 `> version:` 块 + §3 目录树/§3.1/§3.2 标题与正文 inline `[vX.Y]` 噪声。
- `[P0]design_system.md` / `[P0]sse_channel.md` / `[P0]tech_stack.md` 清理 inline 版本号（散文中的 `v0.0.X 起/新增/修订/落地修正/T5` 等），迁移到 frontmatter `since` 或本 log。
- `[P0]sse_channel.md` 删尾部 `## 9. 版本` 段（version 1.0-1.6 历史），保留「isSessionActive 归属层决策史」3 候选方案对比。

## 历史版本详情

- `session_meta` 广播 topic + isSessionActive 调用方改 session 层：详见 `specs/tech/version_logs/v0.0.27/`（session-meta-broadcast-decision.md / unread-model-decision.md）。
- chat 页 session_panel 订阅接线（sessionRunning 权威源切换）：详见 `specs/tech/version_logs/v0.0.13/`。
- 各 topic replayable 配置总表 + session_panel bus 从 replayable=true 修正为 false：详见 `specs/tech/version_logs/v0.0.30/`。
