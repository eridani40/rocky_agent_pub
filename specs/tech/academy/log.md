---
type: log
title: Academy 子系统变更日志（位置轴）
priority: P0
updated: 2026-08-04
---

# Academy 子系统 — 变更日志

| 版本 | 类型 | 改动 | 文件/章节 |
|---|---|---|---|
| v0.0.250 | spec 同步 | **inheritMemory dead field 清理**（squad KB 主场，academy KB 顺带清引用）：`[P1]squad_derive.md` §2.1 CreateMemberInput 删 `inheritMemory?: boolean`（squad schema 字段已删，本文件类型展示同步收缩）+ §6 边界表 derive mode 描述改述（去「`inheritMemory` 半成品」，加「复制父成员个人 AGENTS.md，不拷贝 ws/memory」）。**derive_academy 落点不动**（v0.0.232 团队盘 + v0.0.233 预检裁决既定）。 | `[P1]squad_derive.md` §2.1 + §6 |
| v0.0.233 | 增强 | **derive_academy 派生升级：继承预检 + 同名裁决**（req `reqs/[working] v0.0.233.derive_conflict_resolve/req.md`；PRD `specs/prd/version_logs/v0.0.233/change_log.md` D1-D8 + P1-P5）。把 derive_academy 从「一次性自动 copy + 同名覆盖」升级为**继承预检 → 同名裁决 → 执行**三段（v0.0.232 重映射 seed 落点后浮现的「同名静默覆盖」问题）。**四项落地**：① 新预检 endpoint `POST /squad/:id/member/derive-academy/preview`（纯只读返 PreviewResult：agentsMd.exists + skills/memory Array<{name, sameNameConflict}>；复用 resolveAcademyDeriveIdentity 校验）；② hire body derive_academy 分支加可选 `resolution?: {skills?, memory?}`（per-item `{name, action:'skip'\|'overwrite'}`，**默认全 skip 同名 + 用户逐项改 overwrite + 不同名 merge**，undefined 向后兼容）；③ `seedMemberWorkspaceFromVersion` 改造——抽 `enumerateVersionSource` helper + 新 `previewDeriveAcademySeed` 函数 + `copyDirTracking` 改 `copyDirTrackingConditional`（逐顶层项 conditional copy：同名 + skip 跳过 / 同名 + overwrite 复制 / 不同名复制）；④ **补偿安全不变量固化**（written 只记本次实际写入项；skip 不入 written → 补偿不误删 squad 团队盘原有同名项；written 永不含团队根 `.rocky/skills\|memory\|agents` 目录本身）。AGENTS.md 个人差异文件（带 memberId）无同名概念，预览仅标 exists、seed 直接 copy。memory 落点 / 共享语义不变（团队盘 group scope 全队共享，v0.0.232 既定），本版本只加同名保护。前端 component-derive-academy-picker 加继承预览面板（清单分组 + 同名 amber + 覆盖 toggle + 同名计数提示）。**spec reframe**：`[P1]squad_derive.md` 从 v0.0.210 旧设计（个人盘 + §5.2「独立演化」绝对化）reframe 到 v0.0.232+v0.0.233 终态（团队盘落点 + 预检/裁决机制 + §5.2 按资产类型分层成立）；本版本新增 `[P1]derive_preview_conflict.md` 只描预检+裁决机制细节（不取代 squad_derive.md 总语义）。 | 新增 `[P1]derive_preview_conflict.md`（机制设计）+ index.md ④ 不变量（补偿只删 written）+ §③ 关系图加新文件 + specs/api/overall/11a-squad-endpoints.md §2.1（derive_academy 现状化 + resolution）+ §2.5（新预检 endpoint）+ specs/ui/components/academy-page/component-derive-academy-picker.md（继承预览面板契约） |
| v0.0.230 | 修复 + 收敛 | **群体级默认模型必须选具体模型、无应用层默认概念**（用户澄清）：① 教室 head 默认模型 slot 去「跟随应用默认」继承选项（ModelPicker 去 inheritLabel/onInherit，对齐 squad manageTab 无继承形态；未配显「选择 model」占位）；② 新建教室表单默认模型必选（表单层校验拦截 + 后端天然 400 model_not_configured 兜底，`section-classroom-list` 表单 + `page-academy.handleCreateClassroom` 透传复合 `{providerId, modelId}`）；③ academy 运行时链收窄（`model-resolver.buildFallbackChain` academy 分支删 app 默认第三档 → session → classroom.defaultModel → throw；`resolveModel` throw 按 academy 给引导文案「教室未配置默认模型，请先在教室设置中选择一个具体模型」；`academy-session-model` 注释同步）——建学生（`createStudentWithInitialVersion`）/ 建任务（`createTrainingTaskAndCoach`）在教室未配 defaultModel 时同样 400 model_not_configured（预期收紧）；④ 配置页 KeyModelPicker trigger 补 `provider / model` 前缀（对齐 ModelPicker / InputModelPicker，`lib/providers.ts formatModelDisplay` 口径；落盘纯 modelId 契约不变）；⑤ i18n 删 `classroom.defaultModelInherit`、加 `classroom.createRequireModel`。 | [P0]academy_overview.md §2.1 + [P0]data_model.md §2/§2.1 + [P0]session_kind_extension.md §5.0/§5.0b + specs/ui/components/academy-page/section-classroom-detail.md（head slot 契约）+ section-classroom-list.md（创建必填契约）+ specs/api/overall/18-academy.md §1.1/§2.1/§7 + `../agent/providers_and_models/[P0]model_resolve.md`（academy 两档链） |
| v0.0.223 | 配置 | academy×3 parent.main profile（coach / head_teacher / student）toolBound 加 `todo`（session 级双层待办工具，v0.0.223 新增，绑全部 parent.main；subagent/consolidate/summary 不绑）。权威 `../agent/tools/[P1]todo_tools.md` §5。 | `app/plugins/session-types/academy-*.parent.main.yaml` |
| v0.0.221 | 重构 + 修复 | coach 主权 + 两轴模型（生产⊥归档）+ 两工具拆分 + 3 BUG 修复（架构决策 `reqs/[working] v0.0.221.coach_enhance/design.md`）。**两轴解耦**：生产轴（task，coach 绝对主权，三态机 pending/running/paused+pausedReason，maxTurns 硬上限不可越过须 update_task 调大续训）⊥ 归档轴（adopt(versionId) 旁路定稿，任意 process 版、可重复、不改 task 状态）。**三工具→两工具**：① `train-student` → 改名 **`manage-task`**（coach 专属，13 action；去 start/stop/accept/reject/propose，加 adopt/pause/resume/history；taskId 隐式=rtc.trainingTaskId）；② `manage-student`（9 action）→ **并入 `manage-classroom`**（head 专属，扩 20 action = 原 dataset/grader/skill 9 + 学生 CRUD 7 + 任务监督 4 `start_task/list_tasks/get_task/update_task`）；manage-student-tool.ts 文件删除，helper 函数留作 manage-classroom import。**lifecycle**：删 proposeTask/acceptTask/rejectTask/stopTask + 新 pauseTask/resumeTask/adoptVersion（adopt 旁路不改状态，可重复产 major 递增 formal）；revise 到顶/早停不再 propose，改 status='paused'+pausedReason。**schema enum 收窄**（破坏性）：TrainingTaskSchema.status 6→3 值（pending/running/paused）+ 新 pausedReason 字段（maxturns/completed/stopped/earlystop）；resumeOnStartup migration 把旧值映射到 paused+reason（done→completed / aborted/rejected/awaiting_confirm→stopped），幂等不删 record。**fork 切基线**：显式 baseVersionId 时同步更新 task.temporaryBaselineVersionId（切基线=基线指针替换，下轮 acceptGate 对新基线）。**mapper**：iteration_state 扩版本谱系+candidate/base workspaceDir 绝对路径+已采纳 formal 列表+resumable 标志；task_status 补 coachSessionId+produced formal count；新增 academy_head_role mapper（head 行为指引：task 内部要效果→send_message 给 coach，别自己伸手）。**3 BUG 修复**：① versionLabel 恒 "0.0"（fork/adopt 未 patchVersionJsonLabel 同步 version.json.versionLabel，新 helper `patchVersionJsonLabel` 只改单字段）；② accept 不同步 student.currentFormalVersionId（adoptToFormal 末尾补 putStudent）；③ academy-version-dir.ts 抽离 skills 读侧子树→新文件 `academy-version-skills.ts`（单向依赖 dir→skills；328→221+120 行满足 ≤300）。**HTTP 路由**：删 /accept|/reject|/stop，加 /adopt|/pause|/resume|/update-task。**前端**：删 section-training-result 整页（采纳改 inline 版本树过程版行尾按钮）；task 三态机对齐（去 awaiting_confirm/done 分支）；paused 终态 pausedReason 显示；versionLabel 显示；back-nav 版本树 refetch 修（BUG-003）。 | index.md ④ 不变量 2/3/6 重画 + [P0]training_engine.md §0-§7（接口+状态机+revise 去 propose+resumeOnStartup migration+投递场景）+ [P0]data_model.md §4（status enum 收窄）+§6（forkVersionWorkspace/adoptToFormal 加 patchVersionJsonLabel+同步指针；academy-version-skills.ts 抽离）+ [P0]train_student_tool.md（改名 manage-task，13 action 矩阵重画）+ [P0]session_kind_extension.md §3.1/§3.2/§4.1/§7（两工具拆分 + head_role mapper 新增）+ specs/api/overall/18-academy.md §2.2/§2.4-2.6/§7（路由+错误码）+ specs/prd/overall/12-academy.md + specs/ui/overall/12-academy.md §5/§6/§9 + 00-app-guide.md §3.3（inline 采纳+续训态）|
| v0.0.219 | 修复 + 增强 | Academy 学生详情/训练观察 5 问题修复 + coach 持续可达不变量（PRD `specs/prd/version_logs/v0.0.219/prd.md`，change_plan `specs/tech/version_logs/v0.0.219/change_plan.md`，7 修复区锚点见 `states/v0.0.219/context.md`）。**7 区**：① 1b 版本树过程版归属——`buildVersionNodes`（version-tree-nodes.ts）process 节点 parentVersionId 改按 label major 段匹配 formal（不沿用 parentFormalVersionId，因 multi-turn round2+ base 是 process 非 formal，致 round2+ 过程版落 orphanProc 列表尾脱离 base formal）；② 1c 采纳自溯源——StudentVersionSchema 加 `adoptedFromProcessVersionId:ulid,required:false` + adoptToFormal 写入 + API meta 返 + formal 副标题显「采纳自 v{label}」（初始 0.0 无）；③ 2/C coach 持续可达——前端轮询兜底（useStudentDetail/useClassroomDetail 检测 active task 时 startTimer ~5s + onTick mutateCtx 软刷新，复用 use-training-task 模式，禁 runInit 堆叠 timer）+ 渲染门从 activeTask 单条改最近 N=3 条任务含终态（含 aborted 复盘价值，每张卡保留「进入观察」入口）；后端 `handleGetStudent` response 加 `tasks` 供 useStudentDetail 自足检测 active；④ 3 memory——`resolveVersionContent` 扩读 `.rocky/memory/` md 文件返 `memoryEntries: MemoryEntrySummary[]` + API §1.8 content.memory 从恒 [] 升级真实条目 + Memory 卡显条目+「查看」+ 新建 component-version-memory-modal（只读，对齐 chat-page memory modal）；⑤ 4 移除 Tools 卡（五元组→四元组，仅删 UI 入口，version.json.tools 装配字段保留）；⑥ 5 baseline picker——modal 接 formalVersions + defaultBaseVersionId，PickerRow 加 cycle 切任一 formal，section 不预锁 currentFormal，page-academy 传全 formal 列表；⑦ 6b 任务名版本挂钩——后端 `handleGetClassroom`/`handleGetTask`/`handleCreateTask` task DTO 反规范化 `baseVersionLabel`（getVersion 读 label，教室 tab 无 versions 上下文），前端 6 处任务名改版本前缀「v{baseMajor}.{seq} 训练任务」+ 过程版节点用 3 段 versionLabel + i18n zh-CN/en 同步。**spec↔code 漂移修正**：[P0]data_model.md §6 adoptToFormal 签名对齐代码实际 `(store, root, classroomId, processVersionId)` + 返回加 newWorkspaceDir；api §1.7 response 加 tasks；api §1.8 memory/adoptedFromProcessVersionId 落字段；api §2.2 task DTO 加 baseVersionLabel；api §6 标「前端轮询兜底，SSE 后置」。**架构决策（已锁定）**：实时性选前端轮询（不用后端 SSE，spec §6 SSE 后置）；采纳自视觉=formal 副标题位（不发明组件）；任务名格式「v{major}.{seq} 训练任务」；N=3 最近任务含终态。 | [P0]data_model.md §3/§6 + specs/api/overall/18-academy.md §1.7/§1.8/§2.1/§2.2/§6 + specs/ui/overall/12-academy.md §4/§5/§9（doc-modifier 同步）+ index.md ④（无新原则——本版是 spec 对齐 + 产品不变量升级） |
| v0.0.216 | 修复 + 收敛 | ① **运行时模型解析补 academy 分支**（存量 gap）：`model-resolver.ts` sessionType 扩 `'academy'`，三档链 session → classroom.defaultModel → app 默认（与创建链等价）；`resolveAcademySessionModel` 改调同一 resolver（explicit 不可用继续下探 classroom 档）；bootstrap 闭包复用 academyContext.classroom 零新增 IO。② **academy 4 chat 消费方（班主任/教练/版本会话/subagent 只读）迁统一装配层 `SectionChatSession`，能力全开**（HITL 两卡/abort/两 picker/enqueue/minimap/悬浮菜单从无到有）；删 `component-academy-chat-col` / `use-academy-chat-usage` / academy 平行版 subagent 树 / `use-resizable-col`（→ common `use-persistent-width` + `academy-col-widths`）。前端契约见 `../app/frontend/[P0]chat_session_assembly.md` + api `04a-session-chrome.md`。 | [P0]session_kind_extension.md §5 + frontend KB + ui academy-page/_overview.md |
| v0.0.215 | 修复（实现偏离 spec） | head 学生可见性修复（1 mapper + 1 tool）：① 实现 `AcademyContext.students` head 分支已填充但无 mapper 消费 → 新 `academy-classroom-students` mapper（head scope，注入名单：名字/id/正式版 label+versionId/版本数/在跑任务）+ `academy-context.ts` head 分支补逐生 `safe(getVersion)` 解析 formalVersionLabels；② head 无学生 CRUD/版本读取工具 → 新 `manage-student` head 专属工具（9 action：list/get/create/update/delete student + list_versions/get_version 五元组 + start_training/training_status）；③ start_training 薄壳调 `createTrainingTaskAndCoach`（复用 `train-student-actions` 导出的 `buildCoreDeps`/`coreErrorToResult`）；④ create_student 抽 `academy-student-core.ts createStudentWithInitialVersion` 统一核心（HTTP + 工具两入口，仿 `createTrainingTaskAndCoach` 模式）；⑤ delete_student 在跑任务守卫+级联硬删（CrudStore 查询侧不过滤 _deleted）；⑥ `academy-classroom-role.ts` head 分支扩多行（manage-student 职责 + 训练产出五元组语义 + 分工）；⑦ **用户裁决**：coach toolBound 加 write（candidate 修订刚需）、student toolBound 加 write+edit（默认给基础读写，除非特意收窄——推翻旧「学生不改 workspace」假设），原注释删；⑧ register: `plugin.json` extImpls + `scopes/academy-head_teacher.parent.main.yaml` mapper chain + `session-types/academy-head_teacher.parent.main.yaml` toolBound + `tools/registry.ts` defaultTools。新增 AT case `head-manage-student`（head 决策链全链路 + 防假能用认输话术硬断言）。两 pre-existing known-issue（BUG-001 + propose→head 未接通）记 change_log 不阻塞合并。 | index.md（④原则无新增——本版是修复偏离，非新概念）+ [P0]session_kind_extension.md §3.1/§3.2/§3.3/§4.1/§5.0b（新）/§5.2/§7.1 + [P0]train_student_tool.md §2.7/§4 备注 + specs/api/overall/18-academy.md §6 + specs/ui/overall/00-app-guide.md §3.3 |
| v0.0.214 | 修复（实现偏离 spec） | skills 呈现回到「目录 + 文件（不限 markdown）」本义：① 读侧 `content.skills` 从目录名列表升级为 `SkillSummary`（目录 + 文件树 + 每文件 sha1 hash + description），填 api §1.8 引用却从未定义的类型空洞；② 新增版本 skill 单文件读/写端点（api §1.11），**不复用** `/skill/:name/tree`\|`file`（那边 lookupScope 会回落 app/builtin、需把 workspaceDir 绝对路径塞 URL、且无 formal/process 权限语义）——复用发生在原语层（buildFileTree / parseSkillDir / 新抽 `skills/file-io.ts`）；③ 关闭「skill 目录名列表当 agentsMd 提交 → 覆盖 AGENTS.md」的数据丢失写路径（Skills 卡彻底离开 md 编辑器通道，写走 §1.11 且只覆写已存在文本文件）；④ 采纳 diff 升两级粒度（skill 目录 added/**removed**/modified/unchanged × 文件级，含 SKILL.md 外附属文件），修「永不产 modified + 被删 skill 误标不变」。 | [P0]data_model.md §3.1/§6.1 + [P0]academy_skills.md §9 + index.md ④ 原则 10/11 |
| v0.0.213 | 重构 | coach 训练闭环修复 + 任务牵引 + 两入口统一 + 版本号 3 段化（架构决策 `reqs/[done] v0.0.213.coach/diagnosis.md`）。① **训练闭环改 coach 主导**：废弃 runTurn 自动一气呵成（candidate 是 base 空副本从未被修订的假迭代），引擎暴露 evaluate/revise/forkCandidate/propose 原子 action；去 awaiting_revision 态；acceptGate baseline 语义修正（reviseBaselineAvg：task.temporaryBaseline===base 时返 undefined 首次候选直接采纳不比，否则取 baseline 被采纳时历史 avgScore，修原 getBaselineScore 首次返 0 致恒 improve）。② **任务牵引**：新建 coach session 时投递富任务书 initial user message（学生上下文+candidate ws 路径+directive+工作流指引+train-student action 说明）；新增 academy_coach_role mapper 注入稳定教练职责+方法论+skill 指针；iteration_state mapper 注入 candidate workspaceDir 绝对路径（修 cwd 错位）。③ **两入口统一**：抽 createTrainingTaskAndCoach 核心（建 task+fork 初始 candidate+建真实 coach session[workspaceDir=candidate ws]+投递任务书），HTTP handler 与 head 工具 start 都调它（消除原 start 占位 coachSessionId 偏离）。④ **train-skill 可见**：3 academy skill 从 app/plugins/skills/ 迁 app/plugins/builtins/skills/（builtin 扫描根，dev/打包一致可见），删 build-plugins academySkillsSrc workaround。⑤ **版本号 3 段**：forkVersionWorkspace label 改 {base顶层major}.{taskSeq}.{round}（修原 4 段 bug）。task schema 加 candidateVersionId 字段。 | index.md ④ 原则 9 + [P0]training_engine.md §0-§7 + [P0]train_student_tool.md §1/§2/§3/§4 + [P0]session_kind_extension.md §4.1/§5 + [P0]academy_skills.md §6/§7 + [P0]data_model.md §3/§4/§6 + [P0]academy_overview.md §3/§8.2/§8.3 |
| v0.0.212 | 修复 | 训练链路两处代码偏离 spec：① BUG-001 建任务后无 kickoff 投递（§7 场景 1 漏实现）→ 补 buildTaskStartedMessage（含 taskId/directive/base/优化式指引）+ handleCreateTask putTask 后 deliverTo(coach) fire-and-forget；coach prompt（academy_iteration_state mapper）防御性注入「任务 ID」行。② BUG-002 无 dataset 任务走 acceptGate 恒 equal → 候选永不晋升 → accept 落 adoptToFormal process-only 校验 500 → runTurn 无 dataset 分支候选直接晋升临时基线（不走 gate）+ acceptTask 前置守卫「无可采纳」→ 新错误码 409 nothing_to_adopt（api 18-academy §7）；前端结果页无提升态禁用采纳。 | [P0]training_engine.md §3.1 实现现状 |
| v0.0.210 | 新建 | academy 子系统全新建立：双引擎架构（agent + 训练引擎）、3 新 session-kind（head_teacher/coach/student）、训练任务状态机、train-student 单工具多 action、评估器/数据集体系、3 academy skill、squad derive 扩展。旧 v0.0.183 academy 已于 v0.0.208 整体删除，本版当全新功能设计。 | index.md + [P0]academy_overview.md + [P0]data_model.md + [P0]session_kind_extension.md + [P0]training_engine.md + [P0]train_student_tool.md + [P0]evaluation.md + [P0]academy_skills.md + [P1]squad_derive.md |
| v0.0.210 | spec 对齐 | 实现偏离 spec 统一对齐（doc-sync 阶段 5）：① data_model §1 落盘路径改 CrudStore 平铺（非嵌套）+ entity 复数化；② data_model §6 AcademyStore 拆 store.ts(CRUD) + store-ops.ts(业务原语)；③ data_model §6 forkVersionWorkspace base 允许 formal 或 process（曾误加 formal-only 校验致 multi-turn 500）；④ training_engine §2 deps.llmCaller → llmPort（AcademyLlmPort 窄端口，bootstrap 适配）；⑤ training_engine §5 sample/grade 走 llmPort + 自实现 p-limit；⑥ training_engine §7 sender.source='system'（非 'agent'）；⑦ session_kind_extension §4.1 mapper 路径改 builtins/rocky_context/prompt/；⑧ session_kind_extension §4.1 groups.json 未改（plugin.json extImpls + scope impls 激活）；⑨ session_kind_extension §5.1-5.2 加 academyContext 注入链 + 读侧投影链 4 处；⑩ session_kind_extension §8 AcademyHandlerDeps 加 dataDir+appConfig；⑪ train_student_tool §2.1 start 实现分工（工具层占位 / handler 建真实 coach）；⑫ academy_overview §2.1 加 classroom.defaultModel；⑬ 不存在 training.* SSE 事件（走 a2a inbox）；⑭ GET /academy/session 未实现（走 ?biz=academy）。 | [P0]data_model.md §1/§6 + [P0]training_engine.md §2/§5/§7 + [P0]session_kind_extension.md §4.1/§5/§8 + [P0]train_student_tool.md §2.1 + [P0]academy_overview.md §2.1 |

> 跨版本发布说明见 `../version_logs/vX.Y/change_log.md`。
