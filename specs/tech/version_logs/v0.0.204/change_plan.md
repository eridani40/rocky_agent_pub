# v0.0.204 change_plan — agent session type 与行为契约统一（终版概念：forked 退役 + runKind 扁平 + snapshot 可选）

> 行 = 函数/符号级。阶段：【A1】身份层 →【A2】配置层 →【A3】读取收敛 →【B】装配合并（同版本内顺序，非两版本）。
> 概念权威：`specs/tech/agent/session/[P0]session_kind.md`（SK）+ `[P0]session_type_profile.md`（STP）。
> 终版概念修正（v0.0.204）：forked 概念彻底退役 / runKind 扁平枚举 3 值（main/summary/consolidate）/ 原 modeKey 字段并入 runKind 退役 / derivation main→parent 改名 / 手动+自动 summary 同 type 同 profile / snapshot 可选输入（context engine 双路径）。
> 注：原版本含 academy 相关变更（trainer Role / academy-coach session-type 等），academy 已于 v0.0.208 整体删除；本文件保留下方非 academy 变更作为历史记录，academy 相关行已剔除。

## 进度状态（merge dev1 @ 0db0d45c 后重做 — 8990 UT 绿基线）

- ✅【A1】行#10-32 全部完成（T1 verified CONDITIONAL PASS @ 2026-07-24）
- ✅【A2】行#33-39 完成（T2-A 配置层基建：loader+validator+policy+scopeIdOf+23 yaml @ abc568f5/697eeded）
- 🟡【A3】部分完成 — T2-B1（commit dc9b8058）：policy 经 bootstrap 注入；buildSessionConfigFromDeps（行#52）✅；buildMainDeps（行#50）已读 policy；buildForkedDeps（行#51）已读 policy。剩余：删 TOOL_POLICY/Router、resolveTools 重写、modeKey→runKind rename、RunSpec 删白名单对、forkedRun opts 瘦 5 字段、snapshot 双路径、scope extends 链、post-compact 删常量、SessionConfig.scope 死字段、build-plugins copyResources、createSession enabled 门
- ⬜【B】行#58-63 未启动（T3 装配合并）

| 所属模块 | 文件路径 | 函数/符号 | 类型 | 变更内容 | 约束 | 参考 | 影响行 |
|---|---|---|---|---|---|---|---|
| ✅【A1】session_kind | app/shared/src/types/session-kind.ts | `SessionKind` class | 修改 | 瘦身：字段只留 biz/role/derivation/runKind；删 squadId/memberId/parentSessionId 3 字段；删 `toolPolicyRole` getter；新增 `canonicalId()`（4 段：`${biz}-${role}:${derivation}:${runKind}`）+ `isMainRun`（替原 isForked 反向，`runKind==='main'`） | 落盘零迁移（record 仍 biz/role/derivation）；canonicalId 纯拼接零逻辑零分支；无 isForked/forkedRun 命名 | SK §2；终版概念 A | +55/-90 |
| ✅【A1】session_kind | session-kind.ts | `Derivation` 类型 | 修改 | 值改名 `'main'\|'subagent'` → `'parent'\|'subagent'`（parent=非派生顶级） | 全代码 grep `derivation: 'main'\|derivation==='main'` 同步改 parent；K1-K7 引用同步 | SK §1；终版概念 A | ±20 跨文件 |
| ✅【A1】session_kind | session-kind.ts | `RunKind` 枚举 | 新增 | 扁平闭合枚举 `'main'\|'summary'\|'consolidate'` 3 值；shared/index.ts re-export | **原 modeKey 字段并入 runKind 退役消失**（不再有 ModeKey 类型）；原 modeKey='current'→main、'summary'→summary、'memory_extract'→consolidate；K5 校验闭合 | SK §1；终版概念 A | +8 |
| ✅【A1】session_kind | session-kind.ts | `ToolPolicyRole` type | 删除 | 类型 + 导出删除（消费方迁完后删，见行#27） | `Record<ToolPolicyRole,…>` 闭合同步消失 | 拍板 Q5 | -15 |
| ✅【A1】session_kind | session-kind.ts | `SessionContext` interface | 新增 | 实例 ID 字段（squadId/memberId/parentSessionId） | 与 kind 结伴传递、不合并 | SK §3 | +12 |
| ✅【A1】session_kind | session-kind.ts | `validateSessionKindInput` | 修改 | 拆为 `validateSessionKind(kind)`（K1-K5）+ `validateSessionContext(kind, ctx)`（C1-C7）；K5 runKind 枚举闭合 | 调用点仅 createSession+spawn；抛 SessionKindValidationError 不变；**禁止任何 isForked/forked 分支** | SK §5 | ±40 |
| ✅【A1】session_kind | session-kind.ts | `isStudioMainSession` | 修改 | 签名不变（只读 kind，无实例 ID 依赖） | helper 保留；内部 role==='squad' 等谓词不改 | SK §6 | ±2 |
| ✅【A1】session_schema | app/server/src/agent/schema_defs/session.ts | `derivation` enumValues | 修改 | `'main'\|'subagent'` → `'parent'\|'subagent'`（对齐 SK） | schema 闭合 | SK §1 | ±2 |
| ✅【A1】session_store | app/server/src/agent/session-store-core-impl.ts | `getSessionKind`（L134） | 修改 | 投影 slim kind（无实例 ID；runKind 缺省 'main' 由调用点覆盖） | 落盘零迁移；无读兼容层；forked 命名零残留 | SK §4 | ±15 |
| ✅【A1】session_store | session-store-core-impl.ts | `getSessionContext` | 新增 | 投影 SessionContext（record 同名字段直读） | 可与 getSessionKind 合并返 `{kind,context}`（coder 定位） | SK §3/§4 | +15 |
| ✅【A1】session_store | session-store-core-impl.ts | `createSession` 校验调用（L42） | 修改 | 两层校验 + enabled 门：profile 文件存在且 `enabled!==false`，否则 fail fast；derivation='parent' && runKind='main' 才设门 | ValidationError；summary/consolidate runKind 不经此门 | STP §8 | +12 |
| ✅【A1】session_store | app/server/src/agent/session-store.ts | `getSessionContext` | 新增 | interface 加方法签名 | — | SK §4 | +3 |
| ✅【A1】agent-tools | app/server/src/agent/tools/spawn-action.ts | `resolveEffective` 调用链（L89-107） | 修改 | eff 加 `derivation`（input.derivation ?? template.derivation ?? 'subagent'）→ 写 child kind | spawn 路径跑两层校验（K1-K5/C1-C7） | SK §5；终版概念 C（拉起机制决策：spawn 泛化） | +12 |
| ✅【A1】agent-tools | app/server/src/agent/tools/agent-tool.ts | `createChildSessionImpl`（L279-328） | 修改 | derivation/biz 全部从 eff 读（不再 hardcoded `derivation:'subagent'`）；parentSessionId 仅 derivation='subagent' 必填 | bloodline 缺省不变 | SK §2；STP §9#2；终版概念 C | ±20 |
| ✅【A1】bootstrap | app/server/src/bootstrap-agent-phase.ts | `setResolveConfig` 闭包（L119-190） | 修改 | kind/context 分离构造（SessionKind 不再带 ID）；studioContext 用 context 判定注入 | SK §4 | ±25 |
| ✅【A1】bootstrap | bootstrap-agent-phase.ts | `setBuildAgentToolContext` 闭包 | 修改 | rtc 注 `kind`+`sessionContext` | 拍板 Q5 | ±20 |
| ✅【A1】agent-tools | app/server/src/agent/tools/runtime-context.ts | `AgentToolRuntimeContext`（L64） | 修改 | 加 `kind: SessionKind` + `sessionContext: SessionContext` | selfType/parentScope 保留名不改值源（下游 15+ 工具零改） | SK §3 | +10 |
| ✅【A2】config | app/server/src/agent/session-type-profile-loader.ts | `SessionTypeProfileLoader` | 新增 | 读 app/plugins/session-types/*.yaml → id 索引；extends 链解析+逐字段深合并+缓存；`validateAll()`：id 格式/父存在/无环/枚举闭合/toolBound 引用已注册工具 | 文件不存在=类型未登记（createSession 门数据源）；超 300 行拆 validator 单文件；基座三文件 default/summary/consolidate 必存在 | STP §3/§4/§6；终版概念 D | +180 |
| ✅【A2】config | app/server/src/agent/session-type-policy.ts | `SessionTypePolicy` impl | 新增 | `profile(kind)`（合并缓存直读）+ `resolveToolSet(kind, instanceOverride?)`（bound ∩ instanceOverride，保注册序+剔幽灵名） | resolveToolSet 为 P6 唯一真决策；scopeId/各布尔字段不进 interface | STP §6；裁决② | +80 |
| ✅【A2】config | app/plugins/session-types/*.yaml | default/summary/consolidate 三个基座 + 全量类型 profile | 新增 | default.yaml 全字段基线；summary.yaml = summary runKind 共性（drainMode none/persistsRun false/usagePartition summary/toolBound=[]/toolDefinitionsSource host-snapshot/maxIterDefault 1）；consolidate.yaml = 同 summary 共性但 toolBound=[skill_manage,memory_manage]；启用类型各一文件（playground-rocky.parent.main / studio-{squad,leader,mate}.parent.main / playground-rocky.subagent.main / studio-{leader,mate}.subagent.main / 各 main 类型 .summary + .consolidate（空=继承对应基座）） | 每组合一文件；值=现状行为等价（coder 逐个核对 TOOL_POLICY 7 key 现值） | STP §3/§8；终版概念 A/C/D | +22 文件 |
| ✅【A2】config | app/plugins/scopes/*.yaml | 全量 scope 文件矩阵 | 新增+修改 | 新增 playground-rocky.parent.main（system_prompt_mapper 去 squad_role/team_roster/memory_squad/parent_task + system_reminder 去 squad_* 5 项，Q3 治理）、studio-{squad,leader,mate}.parent.main（extends default 空文件）、5 个 subagent scope（persistent store+主链，STP §9#5）、各 summary/consolidate 组合（extends summary / extends consolidate 空文件）；forked.yaml 拆为 summary.yaml + consolidate.yaml 两基座（原 forked 内容分移） | 文件名=id 映射（`:`→`.`）；每组合一份 | STP §5；拍板 Q3；终版概念 D | +18 文件 |
| ✅【A2】config | app/server/src/plugin/scope-config-loader.ts | `ScopeConfigLoader.loadAll`（L116） | 修改 | 解析 `extends` 字段进 ScopeConfig（+`parentScopeId?`） | 旧文件无 extends = 父 default（向后兼容） | STP §5 | +20 |
| ✅【A2】config | app/server/src/plugin/scope-config-provider.ts | `LoadedScopeConfigProvider.resolveSourceScope` | 修改 | 沿 extends 链逐级回退（取代「未激活→default」二级跳） | per-EP 全量替换语义不变；default 恒 root | STP §5；ext_impl_scope.md §5.2 | ±25 |
| ✅【A2】config | app/server/src/plugin/scope-config-validator.ts | `ScopeConfigValidator.validateAll` | 修改 | + extends 父存在/无环校验；exclusive EP 恰好 1 校验沿链取有效列表 | 启动硬失败语义保留 | STP §5 | +20 |
| 【A3】agent-loop | app/server/src/agent/scope-id.ts | `scopeIdOf(kind)` | 新增 | 单行纯拼接 = `kind.canonicalId()` | 零逻辑零分支零路由表（裁决#1：甚至算不上 policy） | STP §2 | +5 |
| 【A3】agent-loop | app/server/src/agent/agent-scope-router.ts | 全文件 | 删除 | `AgentScopeRouter`/`FORKED_SCOPE_ID`/`CURRENT_SCOPE_ID`/`RouterKindInput` 删除 | 调用点（build-deps.ts:197、build-forked-deps.ts:189）改 scopeIdOf；**后续 build-forked-deps.ts 整体删（行#55）** | 裁决#1；STP §9#3 | -93 |
| 【A3】agent-tools | app/server/src/agent/tool-policy.ts | `TOOL_POLICY`/`SHARED_PLAYGROUND_BOUND` | 删除 | 常量一对一迁 session-types/*.yaml toolBound | 值迁移必须逐项等价（coder 核对 7 key 现值） | 拍板 Q5 | -120 |
| 【A3】agent-tools | tool-policy.ts | `resolveTools` | 修改 | 重写为 `resolveToolSet(kind, instanceOverride?)` 委托 SessionTypePolicy；删 enableToolWhitelist/toolWhitelist/mainAllowedTools 三态分流 | 保 allTools 注册序 + 剔幽灵名不变量保留；三层一致（config/schema/exec 同源） | STP §6；tool_policy.md（重写） | ±60 |
| 【A3】agent-loop | app/server/src/agent/forked-reminder-injector.ts | `injectForkedReminder`（file rename optional: `reminder-injector.ts`） | 修改 | 入参改 `(kind, profile)`：三态文案从 profile.toolBound 派生（替代 enableToolWhitelist/toolWhitelist 对）；rename 文件为可选（防 grep 残留） | 注入点不变（cache 前缀之后零污染） | forked_reminder.md §1/§2（spec 待 doc-modifier rename） | ±20 |
| 【A3】agent-loop | app/server/src/agent/agent-manager.ts | `forkedRun` opts（L362-386） | 修改 | 删 enableToolWhitelist/toolWhitelist/maxIter/emit/toolDefinitions 5 字段（类型级收编 profile）；opts 只留 sessionId/config/runKind/snapshot/userMessage/triggerMessage/triggerUsage/observability；rename 函数为 `sideRun`（保留外部 API 名 `forkedRun` 亦可，coder 定位） | 并发检查/snapshot 双 clone 不变；snapshot 可选（snapshot?: ContextSnapshot） | STP §3；终版概念 B | -30 |
| 【A3】bootstrap | bootstrap-agent-phase.ts | `setForkedRunner`/`setConsolidationRunner` 调用点（L281/L317） | 修改 | 删 `enableToolWhitelist/toolWhitelist/maxIter` 写死传参；runKind='summary' / 'consolidate' 由调用点显式传 | summary 零工具 / consolidate 两工具行为由 profile 文件承载，不可漂移；snapshot 必传自动压缩 / 不传手动压缩（caller 决定） | STP §3；终版概念 B | -15 |
| 【A3】plugins | app/plugins/builtins/rocky_context/compact/post-compact-consolidation.ts | `CONSOLIDATION_ALLOWED_TOOLS`（L36） | 删除 | 常量迁 profile；调用点（L124-125）删两字段传参 | ConsolidationRunner input shape 同步瘦 | STP §3 | -10 |
| 【A3】agent-loop | app/server/src/agent/loop-ports.ts | `RunSpec.enableToolWhitelist`/`toolWhitelist` | 删除 | caller-intent 对删除；allowedTools 由 resolveToolSet 产出写 spec | `RunSpec.modeKey` 字段整体删除（并入 runKind，见行#44） | tool_policy.md §6；终版概念 A | -6 |
| 【A3】agent-loop | loop-ports.ts + agent-interface.ts + agent-run-registry.ts + agent-manager.ts + agent-event-types.ts + agent-loop-base.ts + abort-finalize.ts | `modeKey` 字段/类型 | 修改 | 字段 rename modeKey→runKind；类型 string→RunKind 闭合枚举；取值映射 current→main / summary→summary / memory_extract→consolidate | groupKey 字符串格式不变（值域闭合）；forkedRun→sideRun rename 可选；零残留 isForked/forked 命名 | SK §1；终版概念 A | ±35 跨文件 |
| 🟡【A3】agent-loop | app/server/src/agent/build-deps.ts | `buildMainDeps`（L168） | 修改 | router→`scopeIdOf(kind)`；EOS 读 `profile.eosStop`（替 L200-202 kind.role==='squad' 硬编码）；obs sessionKind 改 `kind.canonicalId()`（L212）；工具三件套读 resolveToolSet | 外壳保留不合并（Phase B 才并）；行为等价；无 if forked 分支 | STP §7 | ±25 | T2-B1 已注入 policy（commit dc9b8058）；剩余 router→scopeIdOf + EOS + canonicalId 读取改待续 |
| 🟡【A3】agent-loop | app/server/src/agent/build-forked-deps.ts | `buildForkedDeps`（L153） | 修改 | 同上行 + toolDefinitionsSource=host-snapshot 读 profile + resolveToolSet 新签名（删 enableToolWhitelist 读取 L169-174）；snapshot 可选化（见行#49 context engine） | 旁路 run cache 契约（toolDefinitions=snapshot?.tools，无 snapshot 时完整重建）钉死；本文件 Phase B 整体并入 build-run-deps 删 | STP §3/§7；终版概念 B | ±30 | T2-B1 已注入 policy；**Phase B（行#60）整删此文件，剩余读取改可跳过直接进 T3** |
| ✅【A3】handlers | app/server/src/handlers/session-config.ts | `buildSessionConfigFromDeps`（L117） | 修改 | kind/context 入参分离；resolveTools→resolveToolSet(kind,{tools:subAgentConfig?.tools})；skills 分派读 profile.skillSource；model 分支读 profile.modelHints.readsSquadDefault；maxIter=override(subAgentConfig?.maxIter) ?? profile.runShape.maxIterDefault | sessionType/role 派生（L196-203）由 profile 字段替代；instanceOverride 优先级=override>profile | STP §7 | ±40 | T2-B1 完成（commit dc9b8058，policy 注入 + 旧 resolveTools fallback 保留待 T2-B 整删） |
| 【A3】handlers | app/server/src/handlers/session.ts | createSession handler | 修改 | enabled 门接线（调 store 层校验，profile 存在且 enabled） | 失败返 400 ValidationError | STP §8 | +8 |
| 【A3】build | scripts/build-plugins.ts | `copyResources`（L90-110） | 修改 | + `app/plugins/session-types/` → dist/session-types/ 拷贝（同 scopes 待遇） | 打包护栏：漏拷 packaged 启动硬失败 | STP §11；持续可打包护栏#2 | +8 |
| 【A3】context | app/server/src/agent/context-types.ts | `SessionConfig.scope` 字段（L161） | 删除 | 死字段删（零消费已 grep 验证，偏差#1）；kind slim + 加 `sessionContext?: SessionContext` | bootstrap 注入点同步删 | STP §9#1 | -8 |
| 【A3】context | app/server/src/agent/loop-stage-context.ts | `buildSideRunSnapshot`（runtime 内部 fn） | 修改 | snapshot 可选化：有 snapshot 时 = snapshot.tools/messages；无 snapshot 时 = contextEngine.buildSnapshot() 完整重建（system+messages+toolDefinitions 从 default EP 链产出 + 追加自身 user message） | **snapshot 可选双路径**：自动压缩 caller 必传 snapshot、手动压缩 caller 不传→完整重建；手动/自动 summary **同 type 同 profile 同组装链**（profile 零区分字段） | STP §9#6；终版概念 B（snapshot = run 可选输入） | +45 |
| 【B】agent-loop | app/server/src/agent/build-run-deps.ts | `buildRunDeps` + profile 驱动 LifecyclePort | 新增 | 单装配函数：profile.runShape（drainMode/backgroundPath/persistsRun/touchesStateMachine）+ lifecycleHooks + eventChannel 驱动 RunSpec 装配；Main/Forked 两 LifecyclePort 合并为 profile 驱动单 impl（onRunEnd/onUsage/onInterrupted 按字段分派）；旁路 run wireInitState（ingest reminder+userMessage 到 in_memory store）按 profile 挂接；snapshot 可选化分支（双路径） | 骨架 runReActLoop 零 if kind；旁路 run 不变量（不持久/不碰状态机/onUsage 分区/abort noop）由 profile 字段承载；**零 forked 命名**（ForkedLifecyclePort/MUTED_BUS 命名退役） | agent_loop_unified.md §4；STP §3；终版概念 B | +250 |
| 【B】agent-loop | app/server/src/agent/build-deps.ts | 全文件 | 删除 | MainLoopHandle/MainLifecyclePort/buildMainDeps 并入 build-run-deps | Phase A 已改读取源，删除纯移位 | 拍板：一版做完 | -295 |
| 【B】agent-loop | app/server/src/agent/build-forked-deps.ts + forked-lifecycle-port.ts | 全文件×2 | 删除 | ForkedLoopHandle/ForkedLifecyclePort/MUTED_BUS/buildForkedDeps 并入 build-run-deps（命名退役） | per-run buffer 回收（start() finally clearScopeSession）语义保留 | 终版概念 A | -380 |
| 【B】agent-loop | app/server/src/agent/agent-manager.ts | `activate`/`run`/`forkedRun` | 修改 | activate 与 forkedRun 统一调 `buildRunDeps`；三 map/CAS/shell 按 runKind 分派；modeKey 字段全删（→ runKind） | `run(spec,loop)` 唯一 loop 入口不变（原则#7） | agent_manager.md §1 | ±40 |
| 【B】agent-loop | app/server/src/agent/run-react-loop.ts | stage helper 读取点 | 修改 | wireInitState/wire extras 消费对齐 buildRunDeps 产出（骨架本体零 if kind 不变）；spec.modeKey→spec.runKind | 骨架 4 阶段结构不动 | agent_loop_unified.md §1 | ±20 |
| 【B】tests | app/server/src/agent/__tests__/（tool-policy/session-store-kind/session-config-tool-policy 等）+ app/plugins 相关 __tests__ | 存量 UT | 修改 | kind/policy/resolveToolSet/loader/validator 新签名迁移；新增 profile loader/validator/继承合并 UT；snapshot 可选双路径 UT（同 case 两输入各一组）；手动/自动 summary 同 type 同 profile UT | 自检必须全量 `bun run test`（memory：shared 结构变更下游断言漂移） | coder-shared-structure-selfcheck；终版概念 B | ±220 |

**总计**：约 +1500/-1400 行（含 ~40 个新 yaml 文件）。删除四套旧机制：TOOL_POLICY、AgentScopeRouter（+SessionConfig.scope 死字段 + RunSpec whitelist 对）+ **forked 命名体系整体退役**（buildForkedDeps/ForkedLifecyclePort/ForkedContextPort/MUTED_BUS/isForked/forkedRun/modeKey）。

**关键新增能力**：① context engine snapshot 可选双路径（手动/自动 summary 同 type 同 profile）；② runKind 扁平枚举 3 值替代 modeKey 自由 string。
